# Qwen adapter

Status: Active
Last updated: 2026-08-15

Read `AGENTS.md` at the repo root -- that file is the canonical contract.
Qwen is an adapter, not an alternate authority.

This directory contains:
- `commands/`: command definitions (orchestrate.md)

All Qwen workflows and command executions must adhere strictly to the
universal contract defined in root `AGENTS.md`.
