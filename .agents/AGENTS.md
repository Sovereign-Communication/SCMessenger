# Agents adapter

Status: Active
Last updated: 2026-08-15

Read `AGENTS.md` at the repo root -- that file is the canonical contract.
This harness is an adapter, not an alternate authority.

This directory contains:
- `rules/`: rule trigger pointer (follow-all-rules.md)
- `skills/`: agent skill definitions (build-verify, docs-sync, finalize-checklist, orchestrate)

All multi-agent harness workflows and skills must adhere strictly to the
universal contract defined in root `AGENTS.md`.
