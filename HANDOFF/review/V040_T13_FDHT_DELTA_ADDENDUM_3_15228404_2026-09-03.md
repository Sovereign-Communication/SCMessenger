# V040 T13-FDHT DELTA ADDENDUM 3 -- HARNESS free lane -- 2026-09-04

Target: PR #267 freebuff/v040-t13-fdht-gate
Reviewed head (FINAL APPROVE pin): 80197ef5
Addendum-1 head: 17fa959f (verdict: coverage carries)
Addendum-2 head: 90f37d8f (verdict: coverage carries)
Current head (this addendum): 15228404
Lane: harness free lane (panel-based, deterministic convergence)
Verdict: APPROVE carries -- reviewed content byte-intact at 15228404;
FINAL APPROVE (V040_T13_FDHT_FINAL_APPROVE_QWEN_2026-09-03.md) + addenda
1 and 2 remain valid for PR #267 at the re-updated head.

## Why addendum-3 exists

After #275 (ci.yml cargo-deny pin) merged to main (squash b79702ba,
06:31:03Z), strict branch protection required a THIRD update-branch:
head moved 90f37d8f -> 15228404. Same stop-and-report discipline as
addenda 1 and 2.

## Evidence (verbatim git outputs, origin/main fetched fresh = b79702ba)

1. 80197ef5, 17fa959f, and 90f37d8f are ALL ancestors of 15228404
   (git merge-base --is-ancestor -> exit 0 for each).
2. 15228404 is a two-parent merge: parents 90f37d8f + b79702ba (#275
   squash on main). No PR commit rewritten, reordered, or dropped.
3. Non-merge commits added since addendum-2: exactly one -- b79702ba
   (#275), already-landed and individually gated (CI-file-only).
4. Delta 90f37d8f..15228404 = #275 content only: .github/workflows/ci.yml
   +5/-1 (the cargo-deny pin).
5. PR-own diff at the current head (git diff origin/main...15228404) is
   IDENTICAL to the PR-own diff at the reviewed head (git diff
   origin/main...80197ef5): same 3 files, same counts
   (cli/src/ledger.rs 64, core/src/store/ledger_entry.rs 402,
   core/src/transport/swarm.rs 149, 507 insertions / 108 deletions).
   Note: raw sha256 of the two three-dot diffs differ, but ONLY because
   the three-dot merge-base moved when main advanced via #275 -- the
   panelists independently identified this as a diff-base artifact, not
   a PR-content change (stats byte-identical).
6. ci.yml in the branch is IDENTICAL to main (git diff --quiet exit 0):
   the pinned cargo-deny install is carried, so the Lint job at this
   head runs the fixed toolchain.
7. git merge-tree --write-tree origin/main 15228404 -> exit 0 (no
   conflicts).

## Panel

- minimax/minimax-m3:free -- voted real: false on all 4 claims
  (c1 0.92, c2 0.95, c3 0.90, c4 0.90), finish=stop
- inclusionai/ling-3.0-flash-fin:free -- voted real: false on c1 (0.95)
  and c2 (0.95); truncated (finish=length) before c3/c4 -- no dissents
  in any emitted vote
- google/gemma-4-31b-it:free -- 503 provider error, rotated out
- judge/convergence: cohere/north-mini-code:free -- synthesis: "Approve
  PR #267; the changes are only CI workflow pins carried from main
  (#275) and the original PR content remains unchanged, so the previous
  review remains valid." (confidence 0.93)
- Cost: $0.000000 total (free tier, ceiling enforced)

## Claims manifest and tally

Manifest + verbatim source window: tmp/harness/w267-addendum3-claims.json
+ tmp/harness/w267-addendum3-window.txt (52 lines, sections A-I).
Deterministic tally over emitted parseable votes: 6/6 `real: false`
(minimax 4/4 complete, ling 2/2 emitted) -- zero dissents on any claim
(c1 rewrite/identity, c2 novel files in the third update, c3 PR-own diff
changed, c4 merge conflict/invalidation).

| claim | minimax | ling (emitted) | tally |
|---|---|---|---|
| c1 | false (0.92) | false (0.95) | 2/2 false |
| c2 | false (0.95) | false (0.95) | 2/2 false |
| c3 | false (0.90) | (truncated) | 1/1 false |
| c4 | false (0.90) | (truncated) | 1/1 false |

Consensus: agreement=high, confidence=1.0, disagreements=[severity
classification info vs low; explanation focus], defer=false. The
Section-G sha mismatch was reasoned by BOTH panelists as the three-dot
merge-base artifact of main advancing, not PR content change. Full raw
output: tmp/harness/w267-addendum3-verdict.json.

## Autonomy ledger

- Chain verified: true (harness ledger verify, first_bad_seq null)
- Entries: task_id scmessenger-267-addendum3, ledger count 186

## Verdict

APPROVE for PR #267 @ 15228404. The FINAL APPROVE at 80197ef5 carries
byte-intact through all three update-branch moves. Merge may proceed once
the required CI is green at 15228404 (Lint now runs the pinned
cargo-deny install carried from main via #275). Review-lane hold remains
CLOSED.