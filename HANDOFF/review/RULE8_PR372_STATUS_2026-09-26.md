# Rule-8 status and operator decision record -- PR #372

<!-- HANDOFF-SCOPE-BEGIN -->
scope: SCMessenger
owner: Sovereign-Communication/SCMessenger
purpose: SCMessenger-only findings and remediation handoff
foreign_material: NONE
boundary: No foreign-repository findings, evidence, status, or remediation are included.
<!-- HANDOFF-SCOPE-END -->

This document is owned by SCMessenger (Sovereign-Communication/SCMessenger).

Date: 2026-09-26
Recorded by: Freebuff lane (the lane that authored D1/D9 and assembled the evidence pack)
Branch: `fix/361-review-blockers`

## STATUS: THE RULE-8 GATE IS STILL OPEN. NOTHING BELOW CLEARS IT.

This record advances the step to the point where the only thing missing is a
decision the author lane is not permitted to make. It is deliberately NOT a
clearance, NOT an approval, and NOT a review. The author of the reviewed work
is the party writing this, which is disclosed here rather than hidden, and is
precisely why nothing in this document can substitute for the reviewer Rule 8
requires. See `docs/rules/SECURITY_PROTOCOL.md`, "Reviewer independence".

## 1. Current PR state (verified by command, not from memory)

| fact | value | how it was read |
|---|---|---|
| PR | #372, OPEN, not draft | `gh pr view 372` |
| head | `cc4731b66bcfd929a93fe252f7424d5d50afe711` | `gh pr view --json headRefOid` |
| base | `main` | same |
| mergeable | `MERGEABLE`, `mergeStateStatus: BEHIND` | same |
| scope | 77 files, +18076 / -36 | `git diff --stat origin/main...HEAD` |
| check runs on head | 33 total, 33 success, **0 failure** | check-runs API |
| `Handoff ownership scope` runs on head | **0** | check-runs API, filtered by name |
| branch behind main by | 6 commits | `git rev-list --count cc4731b6..origin/main` |

### Two mechanical blockers that are independent of Rule 8

Both are new consequences of work landed after this branch head was written,
and neither is a judgement call. Both must be cleared before any reviewer can
sign a mergeable head, and both are cleared by the same action.

1. **`Handoff ownership scope` has never reported on this head.** The head was
   committed `2026-09-25T10:04:21-10:00`. The workflow that provides that check
   landed on `main` at `2026-09-25T13:04:44-10:00` as part of #375, roughly
   three hours later, and it was then added to the required contexts. A commit
   that predates the workflow cannot produce the check, so branch protection
   will wait forever for a status that cannot arrive. Any new commit on this
   branch re-runs the full workflow set and the check reports normally.
2. **The branch is `BEHIND` and `strict=true` is set on `main`.** `main` has
   moved to `cd511fa028029bf8fe1e64b2865709086fc0090a`. The branch must merge
   `main` in before it can merge back.

3. **The gate FAILS on this PR, and it is not a small surface.** This is
   confirmed by CI, not predicted: on head `e2aa379f` the required
   `Handoff ownership scope` check ran and returned **failure**. The gate checks
   CHANGED handoff documents, and this PR changes **22** of them, of which 16
   are prose. Measured against the current branch copies with the gate from
   `main`:

   | | count |
   |---|---|
   | prose handoff documents changed by this PR | 16 |
   | **failing the gate** | **15** |
   | passing | 1 (this file, which was written to pass) |

   | verdict | count |
   |---|---|
   | `scope metadata must occur exactly once ... (found 0 begin markers, 0 end markers, 0 valid blocks)` | 14 |
   | `handoff body does not identify its owning repository` | 1 |

   The 14 are: `HANDOFF/harness/JEV_DOGFOOD_RUN_2026-09-22.md`,
   `HANDOFF/plans/OC_DOGFOOD_INCORPORATION_PLAN.md`,
   `HANDOFF/plans/RETICULUM_AUDIT_2026-09-21.md`,
   `HANDOFF/plans/V040_READINESS_3NODE_CONFIRMATION_2026-09-22.md`,
   `HANDOFF/plans/V050_PHILOSOPHY_AND_BORROW_PLAN.md`,
   `HANDOFF/review/D1_D9_HARNESS_ADVERSARIAL_FINDINGS_2026-09-22.md`,
   `HANDOFF/review/OC_LANE_AUDIT_2026-09-22.md`,
   `HANDOFF/review/RULE8_PR372_RESOURCE_ACCOUNTING_2026-09-25.md`,
   `HANDOFF/todo/D2_SEED_DIAL_REDIAL_MISSING_SEED_PEER.md`,
   `HANDOFF/todo/D3_SELF_RECIPIENT_POISON_GUARD.md`,
   `HANDOFF/todo/D5_D6_BOOTSTRAP_NODES_PERSISTENCE.md`,
   `HANDOFF/todo/D8_CUSTODY_DELIVERY_RECEIPT_STATUS.md`,
   `HANDOFF/todo/D9_LIBP2P_EITHER_HANDLER_PANIC.md`, and
   `HANDOFF/todo/_QUEUE.md`. The 15th is
   `RULE8_PR372_PANEL_FINDINGS_2026-09-25.md`, which has a scope block but no
   body owner alias.

   None of these failures involves a foreign product alias. Every one is the
   missing metadata that `scripts/backfill_handoff_scope.py` adds to prose
   handoffs, and every one is a consequence of this branch having diverged from
   `main` before #375 and #384 landed. Merging `main` in resolves the documents
   that exist on both; the branch-only ones still need the script run over them.

   Note that `D1_D9_HARNESS_ADVERSARIAL_FINDINGS_2026-09-22.md` appears here. Its
   disposition has been awaiting the operator for some time, and it is now also
   a mechanical blocker on this PR: it cannot land without a scope block, and
   its provenance question is unresolved.

   Not applied by this lane. Stamping a handoff declares an owner, and for the
   Rule-8 evidence documents that is this lane's own evidence pack. The remedy
   is one command after `main` is merged in, or an owner's hand.

None of these blockers is a reason to reject the work. All three are sequencing
or mechanical compliance, and the reviewed code is unaffected by any of them.

## 2. What the independent panel DID establish

The panel (`Harness verify`, fusion_lite, 3/3 seats, judge `z-ai/glm-5.3-flash`,
no paid route used) reviewed the D9 vendored delta in two rounds.

- Round 1, head `08c3482f`: **BLOCK**. Agreement `medium`, confidence 0.75.
- Round 2, head `ec5f0135`: no critical or security-regression blocker.
  Agreement `medium`, confidence **0.62**, verdict `defer`, conditional on
  closing two medium findings: Q1 (drop-site logging must be at `warn`/`error`
  with operator-triageable messages) and Q3 (per-site acquire/release
  balance-neutrality at the eight converted drop sites).

The panel established something the author lane had not: that the round-1
"missing regression tests" finding was **false** -- the tests exist in
`handler/either.rs` under the default feature set, and the panel said so rather
than guessing. It also converged that the Q2 Option conversion is sound and
that the Q4 `ProtocolsChange::Added` panic is cfg-gated and non-operational.

### The round-2 findings still apply to the current head

The panel reviewed `ec5f0135`; the branch is now at `cc4731b6`. That gap was
checked rather than assumed:

```
git diff --name-only ec5f0135 cc4731b6 -- vendor/ core/src/transport/   -> 0 files
git diff --name-only ec5f0135 cc4731b6                                    -> 6 files, all under HANDOFF/review/
```

**No reviewed code changed between the reviewed head and the current head.**
The six files that differ are the panel findings document, the resource
accounting document, and four evidence artifacts. So the round-2 technical
verdict is applicable to `cc4731b6` as it stands, and a reviewer signing
`cc4731b6` is signing the same code the panel read.

## 3. What the panel CANNOT establish

This is the part that matters, and it is not a criticism of the panel.

- **It cannot satisfy Rule 8.** Rule 8 requires a verdict from a reviewer who
  did not author D1/D9. The panel is explicitly not a substitute, and the
  panel's own findings document says so in its own STATUS line. Agreement
  `medium` at confidence 0.62 is also below the operator's stated 99% bar, so
  even taken at face value the round-2 result is a defer, not an approval.
- **It cannot resolve the open question in `D9_LIBP2P_EITHER_HANDLER_PANIC.md`**:
  whether a remote peer can induce the desync at all. The panel's own words are
  that this "cannot be settled by reading code". If the answer is yes, the drop
  class has to be re-derived and Q1/Q3 move toward critical -- which would
  invalidate the current remediation direction. No amount of further static
  review settles it; it needs a runtime answer.
- **It cannot close Q3 by argument.** The panel accepted either a per-site
  resource-accounting argument in the review OR an instrumented run.
  `RULE8_PR372_RESOURCE_ACCOUNTING_2026-09-25.md` supplies the argument, but
  the argument was written by the author lane. It is not independent, and the
  panel has not read it. The one seat that asserted a leak with confidence was
  not outvoted; the other two declined to assert it and put the burden of proof
  on the patch. That question is still open in both directions.
- **It cannot speak for the current base.** It reviewed against
  `a76d7d66`. `main` has since moved 6 commits.

## 4. Defects found in this evidence pack while verifying it

Recorded because a pack that cites paths which do not resolve is not evidence.

- **Every path in the "Evidence locations" section of
  `RULE8_PR372_PANEL_FINDINGS_2026-09-25.md` is wrong.** It cites
  `tmp/rule8-pr372/pack/...` and `tmp/rule8-pr372/source/...`; all four cited
  locations do not exist. The evidence is committed at
  `HANDOFF/review/rule8-pr372/evidence/`. Corrected in that file by this commit.
- **`prompt_d9_vendor.txt` is not in the repository at all.** The exact prompt
  sent to the panel exists only in the working tree, so the review is not
  reproducible from the repository: a reader cannot re-run what the panel read.
  It is not merely untracked, it is **excluded by `.gitignore` line 29
  (`*.txt`)**, so `git add` will not take it. Adding it requires either
  `git add -f` or a `.gitignore` exception.

  It is NOT added by this commit. Force-adding a file the repository
  deliberately ignores, or editing the ignore rule, is a policy decision for
  the owner, and it is recorded here rather than taken unilaterally. The file is
  6454 bytes and its content is the review input for SCMessenger's own vendored
  tree, so it is not foreign material; only the ignore rule stands in the way.

## 5. The concrete remaining action, and it is the operator's

There is no further step the author lane can take. Rule 8 is a gate about
reviewer independence, and the author lane is disqualified by construction.
Pushing harder on analysis cannot move it, and the author lane declining to
sign is the correct behaviour rather than an obstruction.

**Required, in this order:**

1. **Sequence the branch** (no review judgement involved): merge `main` into
   `fix/361-review-blockers` and push. That clears the `BEHIND` state and
   produces a head on which `Handoff ownership scope` reports, satisfying the
   required context. The reviewed code is unchanged by this, per the invariance
   proof in section 2, so a reviewer is still signing the code the panel read.
   Then clear blocker 3: merge `main` in first (which brings the stamped copies
   of the 14 shared documents), then run
   `python scripts/backfill_handoff_scope.py --repo-root . --apply` for the
   branch-only ones, and confirm the gate is green on the new head. Blocker 3
   must be cleared before a reviewer's head is mergeable, or the review is
   performed on a head that cannot land.
2. **Obtain an uninvolved reviewer's verdict on that new head.** The options,
   all of which are the operator's to choose:
   - an uninvolved human reviewer records an APPROVE; or
   - the operator reviews and signs directly; or
   - the operator authorises the paid Hourglass escalation that was already
     routed, which is the path the earlier panel result was escalated toward.

   Note for whichever is chosen: Q3 and the remote-desync question are the two
   items a reviewer must actually rule on. A signature that does not address
   them reproduces the defer rather than clearing it.

**Not required, and not to be done by the author lane:** approving the branch,
merging it, deploying, tagging, or running a paid review service.

## 5a. Mechanical blockers: cleared, and the residue that is not mechanical

Recorded 2026-09-26 after head `a74563fb`. Gate state on this head: **RED**,
15 failing documents reduced to **9**. The Rule-8 gate remains OPEN.

Cleared, all mechanical:

1. `Handoff ownership scope` now reports. The earlier heads predate the
   workflow, so the required check could never produce a status.
2. `main` is merged in (clean, predicted by `merge-tree` before acting). The
   branch is no longer BEHIND.
3. Ten handoff documents brought into compliance by
   `scripts/backfill_handoff_scope.py` (9 blocks + 1 ownership sentence).

Reviewed code is provably untouched. Identical subtree hashes before and after
the merge: `vendor/libp2p-swarm-0.48.0` = `c0b1fba5`,
`core/src/transport` = `ef003972`, and the two D9 files the panel read,
`handler/either.rs` = `24814d2d` and `behaviour/either.rs` = `63f2ff29`. Zero
files differ under either path versus `ec5f0135`.

### The nine the tooling cannot clear

| # | document | why the backfill refuses |
|---|---|---|
| 1 | `HANDOFF/harness/JEV_DOGFOOD_RUN_2026-09-22.md` | cites foreign review tooling |
| 2 | `HANDOFF/plans/OC_DOGFOOD_INCORPORATION_PLAN.md` | cites foreign review tooling (two aliases) |
| 3 | `HANDOFF/review/D1_D9_HARNESS_ADVERSARIAL_FINDINGS_2026-09-22.md` | cites foreign review tooling |
| 4 | `HANDOFF/review/OC_LANE_AUDIT_2026-09-22.md` | cites foreign review tooling |
| 5 | `HANDOFF/harness/packs/scm-ops-issues-v1.json` | not prose; a comment breaks JSON |
| 6 | `HANDOFF/review/rule8-pr372/evidence/d9-vendor-delta.patch` | not prose; a comment breaks a patch |
| 7 | `HANDOFF/review/rule8-pr372/evidence/d9_panel_result.json` | not prose |
| 8 | `HANDOFF/review/rule8-pr372/evidence/d9_panel_result_v2.json` | not prose |
| 9 | `HANDOFF/review/rule8-pr372/evidence/vendor_handler_either_v2.rs` | not prose; a comment breaks Rust |

The exact alias strings are deliberately not reproduced here: writing them
into this document would make it a foreign-alias document and trip the same
gate. They are listed in the backfill's own skip output and in
`FOREIGN_ALIASES` in `scripts/validate_handoff_scope.py`.

Rows 1-4 are a product-boundary judgement. Rows 5-9 are the known consequence
of `is_handoff_path` classifying by LOCATION: anything under a `HANDOFF/`
directory is treated as a handoff regardless of suffix, so the gate asks for an
HTML comment inside JSON, a patch, and a Rust source file. The backfill's
prose-only guard already encodes the right answer and the validator does not;
that divergence is a defect in the gate, not in the tooling.

Neither class is a mechanical fix, and neither is decided here. Rows 1-4
require the owner to say whether those documents are SCMessenger's to declare,
and row 3 is the `D1_D9` disposition that has been open for some time. Rows
5-9 need either a narrowed `is_handoff_path` or a decision to relocate the
evidence pack.

### Still required, unchanged by any of this

An uninvolved reviewer's verdict. Nothing in this section, and nothing this
lane can do, substitutes for it. Q3 (per-site acquire/release balance) and
whether a remote peer can induce the desync remain the two questions a
reviewer must actually rule on.

## 6. What this commit did and did not change

Changed: this document; the evidence-location paths in the existing panel
findings document (a factual correction -- the old paths did not resolve).
Nothing else. `prompt_d9_vendor.txt` was NOT added, for the reason in section 4.

Unchanged: all reviewed code. Zero files under `vendor/` or
`core/src/transport/` differ from the head the panel reviewed. No merge, no
deploy, no tag, no paid service, and no change to the shared checkout or to any
other lane's work.
