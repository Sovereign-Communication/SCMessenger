<!-- HANDOFF-SCOPE-BEGIN -->
scope: SCMessenger
owner: Sovereign-Communication/SCMessenger
purpose: SCMessenger-only findings and remediation handoff
foreign_material: NONE
boundary: No foreign-repository findings, evidence, status, or remediation are included.
<!-- HANDOFF-SCOPE-END -->

This document is owned by SCMessenger (Sovereign-Communication/SCMessenger).

{
  "prompt_file": "tmp/a2_durable_delivery_design.md",
  "panel_results": [
    {
      "model": "qwen/qwen3-235b-a22b-2507",
      "content": "**1. Unbounded storage risk?**  \nYes \u2014 **if `DeliveryObligation` is persisted without bounds**. The design mandates indefinite persistence of obligations, but if the user sends 1000 messages per day to an unreachable peer, the database grows without bound. **Correct bound**: Apply **global or per-peer disk quota at enqueue time**, enforced *before* acceptance. For example, reject new messages when total `DeliveryObligation` size exceeds 100 MiB or per-peer obligations exceed 100 messages. This satisfies backpressure-at-acceptance and avoids silent eviction. **Risk**: No bound violates storage philosophy; bound *after* acceptance violates durability. Only pre-acceptance rejection is valid.\n\n**2. Transport-acked but no receipt: retry or not?**  \n**Do not retry** the same message. Transport-ack without receipt implies the peer *received* the message but failed to generate a receipt. Retrying risks **duplicate delivery**. The correct behavior is:  \n- Treat transport-ack as **delivery evidence**, pausing retries.  \n- **But**: If no receipt arrives within a long window (e.g. 7 days), escalate to **manual user intervention** (e.g., \u201cDelivery uncertain \u2014 request receipt?\u201d).  \n- **Duplicate suppression** must be receiver-side: each message must carry a **unique message ID**, and receivers must deduplicate before processing. Without this, duplicates *will* occur. **Risk**: No dedup ID \u2192 duplicates on retry; no receipt \u2192 UI stuck in \u201csent, not delivered\u201d limbo.\n\n**3. Races between backoff and opportunistic triggers?**  \n**Race**: Concurrent triggers (e.g., peer reconnect + network wake) may invoke multiple `flushPendingOutbox` calls, leading to **duplicate delivery attempts**. Worse, if persistence is not atomic, two threads may read the same obligation, attempt delivery, and both increment `attemptCount` without coordination.  \n**Fix**: Serialize using **database pessimistic locking** on `DeliveryObligation` rows. On each trigger, acquire a row-level write lock before scheduling an attempt. Alternatively, use **application-level mutex per peer or obligation ID**. **Risk**: Duplicate sends, inflated attempt telemetry, race in persistence causing lost state.\n\n**4. What must be persisted, and failure mode on partial write?**  \nMust persist:  \n- `DeliveryObligation` (ID, payload hash, peer, timestamp, state)  \n- `lastAttemptTime`, `nextAttemptDue`, `attemptCount` (telemetry)  \n- State transitions: `PENDING`, `DELIVERY_IN_PROGRESS`, `ACKED_NO_RECEIPT`, `RECEIPT_RECEIVED`, `CANCELLED`  \n\n**Failure mode**: If a crash occurs mid-write during `nextAttemptDue` update, the obligation may be retried too soon or too late. **Risk**: **Inconsistent state** \u2014 e.g., timer thinks next retry is in 1h, but DB says 5min, causing missed or duplicated attempts.  \n**Mitigation**: Use **atomic transactions** for state + schedule updates. On startup, reschedule all `PENDING` obligations with `nextAttemptDue <= now` \u2014 this tolerates missed writes.\n\n**5. Cap backoff at 1 hour? Mobile Doze implications.**  \n**No \u2014 1-hour cap is too aggressive** for mobile. Android Doze and App Standby can **delay alarms indefinitely**. A 1-hour cap means attempts stall for days, violating timely delivery. Worse, capped backoff may cause **thundering herd** when device wakes \u2014 all obligations fire at once.  \n**Better**: Use **event-driven triggers as primary**, and **only fall back to timers with floor \u2265 12 hours**, not cap. For example:  \n- Base interval: 2x exponential backoff, jittered, **floor at 12h**, no upper cap.  \n- But **immediately trigger** on peer online, network up, app foreground, etc.  \nThis avoids timer spam while ensuring liveness. **Risk**: 1-hour cap \u2192 excessive wakeups \u2192 battery drain; too long a floor \u2192 perceived deadness.\n\n**6. User-visible states to avoid false \u201cfailed\u201d or \u201ccorrupted\u201d**  \nUI must **never show** \u201cfailed\u201d or \u201ccorrupted\u201d for undelivered messages. Required states:  \n- `Sending` \u2014 enqueued, first attempt not made  \n- `Sent` \u2014 transport-ack received, no receipt  \n- `Delivered` \u2014 receipt received  \n- `Cancelled` \u2014 user cancelled  \n- `Corrupted` \u2014 **only** if `markMessageCorrupted` was called (undecodable)  \n\n**Critical**: \u201cSent\u201d does **not** imply \u201cdelivered\u201d. UI should show \u201cSent, awaiting receipt\u201d \u2014 **not** \u201cFailed to deliver\u201d. **Risk**: Calling `markMessageCorrupted` on retry exhaustion (as in current design) violates philosophy \u2014 that state is for **payload errors only**. If UI shows \u201ccorrupted\u201d due to network issues, it\u2019s a **truth violation**.\n\n---\n\n**Summary of Critical Risks**:  \n- Unbounded DB growth without pre-acceptance quotas.  \n- Duplicate sends without dedup IDs and trigger serialization.  \n- Partial persistence causing missed retries.  \n- 1-hour backoff cap causing battery drain and Doze issues.  \n- UI misrepresenting state due to misuse of \u201ccorrupted\u201d and lack of \u201csent, no receipt\u201d state.  \n\n**Fixes**: Enforce quotas at enqueue, use atomic DB ops, dedup on receiver, serialize triggers, extend timer floor, and refine UI states.",
      "finish_reason": "stop",
      "cost": 0.0007168788,
      "truncated": false
    },
    {
      "model": "deepseek/deepseek-chat-v3.1",
      "content": "## Design Review: Durable Delivery Obligations\n\n### 1. Unbounded Storage Growth\n**Risk:** The design replaces deletion policies with persistent `DeliveryObligation` records. Without explicit bounds, obligations accumulate indefinitely (e.g., users sending to offline peers). This violates storage constraints, even with backpressure at enqueue.\n\n**Solution:** Apply a **strict storage quota** for pending obligations at the *repository level*. Reject new messages when the quota is full (enforcing backpressure). The quota must be based on durable storage metrics (e.g., disk space or record count), not memory. Monitor and alert on quota saturation, but never auto-delete obligations.\n\n### 2. Transport-ACK Without Receipt\n**Risk:** Retrying forever after a transport-ACK risks duplicates; stopping risks non-delivery. This is a fundamental trade-off.\n\n**Solution:** Favor retrying indefinitely. Duplicate suppression must occur on the **receiver** using deduplication keys (e.g., sender ID, message ID, sequence number). The protocol must ensure receipts are idempotent and acknowledgments are unambiguous. If a transport-ACK is received but no receipt, treat it as a transient failure\u2014the recipient may have crashed before persisting. Never assume delivery without a valid application receipt.\n\n### 3. Races Between Triggers and Backoff Timer\n**Risk:** Opportunistic triggers (e.g., peer reconnect) may collide with the backoff timer, causing redundant attempts or starvation.\n\n**Solution:** Serialize scheduling via a single state machine per obligation. Use a **scheduler queue** with a monotonic state variable (e.g., `nextAttemptTime`). On any trigger:\n- If `currentTime >= nextAttemptTime`, attempt immediately.\n- Else, reschedule to the earlier of `nextAttemptTime` or the new backoff interval.\nPersist `nextAttemptTime` to survive process death. Use atomic compare-and-set operations to update the state.\n\n### 4. Persistence Requirements and Failure Modes\n**Must persist:** For each obligation, at minimum:\n- Message payload (or reference to immutable blob).\n- Obligation state (e.g., `nextAttemptTime`, `lastError`).\n- Deduplication keys and protocol metadata.\n\n**Failure mode:** Use a transactional database (e.g., SQLite with WAL). If a write fails during state update, the obligation remains in its previous state. On reboot, recover by resetting `nextAttemptTime` to the backoff ceiling (1 hour) for obligations mid-update. Log errors and retry persistence.\n\n### 5. Backoff Cap and Event-Driven Triggers\n**Cap at 1 hour is too aggressive** for mobile. Doze mode may prevent execution, causing attempts to cluster after wake-up. Instead:\n- Use event-driven triggers (e.g., network available, peer online) for immediate retry.\n- For timer-driven attempts, cap at **24 hours** to align with Doze maintenance windows.\n- Use `WorkManager` or `JobScheduler` to defer attempts until device is charging and online, with a fallback max delay.\n\n### 6. User-Visible States to Avoid Misleading Claims\nNever show \"corrupted\" or \"failed\" for undelivered messages. Instead, implement:\n- **Pending**: Actively retrying (e.g., \"Sending...\").\n- **Held**: Waiting for network/peer (e.g., \"Waiting to retry\").\n- **Cancelled**: User revoked.\n- **Delivered**: Receipt confirmed.\n\nHide technical states (e.g., attempt count). For obligations stuck in \"Held\", add a user option to \"Cancel send\" or \"Retry now\". Use adaptive phrasing: \"Not delivered yet\" instead of \"Failed\".\n\n## Summary of Changes\n- Add a storage quota for obligations to enforce bounded growth.\n- Mandate receiver-side deduplication and indefinite retries after transport-ACK.\n- Serialize triggers via persisted `nextAttemptTime` with atomic updates.\n- Persist obligation state transactionally; recover safely after crashes.\n- Extend backoff cap to 24 hours and leverage event-driven triggers.\n- Replace \"corrupted\" with persistent \"Held\" state and user-actionable cues.\n\nThis ensures obligations are maintained indefinitely without unbounded growth, races, or misleading user feedback.",
      "finish_reason": "stop",
      "cost": 0.0010419651,
      "truncated": false
    },
    {
      "model": "meta-llama/llama-3.3-70b-instruct",
      "content": "### 1. Unbounded Memory/Storage Leak\n\nThe proposed design attempts to address unbounded memory/storage growth by applying capacity limits as backpressure at enqueue time. This means that when the outbox reaches its capacity limit, new messages are rejected before they are accepted, preventing the queue from growing indefinitely. \n\nHowever, the correct bound application to prevent memory/storage leak without violating the philosophy is to ensure that:\n\n- **Enqueue time checks**: Before adding a new message to the outbox, the system checks if adding this message would exceed the storage capacity. If it would, the message is not added, thus preventing the queue from growing beyond manageable bounds.\n- **Persistent Storage**: The `DeliveryObligation` records are stored in a persistent database or file system that does not automatically remove items based on age or attempt count, to indefinitely maintain the delivery obligations.\n\n### 2. Transport-Acked but No Receipt\n\nWhen a message is transport-acked but no receipt ever arrives, the correct behavior is to continue retrying the delivery indefinitely, as per the product philosophy. To mitigate the risk of duplicate delivery, the receiving end must implement idempotence for message receptions. This means:\n\n- **Idempotent Message Handling**: The receiver must be designed to handle duplicate messages (with the same message ID) in such a way that receiving a message more than once has the same effect as receiving it once. This could involve checking a message ID against a set of already processed IDs.\n- **Receipt Mechanism**: It's crucial for the receipt mechanism to be reliable. If a receipt is never sent back, the sender will continue to retry, but this ensures that the message is not silently discarded.\n\n### 3. Serializing Opportunistic Triggers and Backoff Timer\n\nRaces between opportunistic triggers and the backoff timer can occur when an opportunistic trigger (e.g., peer reconnect) and the backoff timer expire at nearly the same time, potentially causing multiple attempts to be initiated almost simultaneously. To serialize these:\n\n- **Lock Mechanism**: Implement a lock or a synchronized block around the code that initiates a new attempt, ensuring that only one trigger (either the backoff timer or an opportunistic trigger) can initiate an attempt at a time.\n- **Immediate Retry**: When an opportunistic trigger occurs, instead of immediately initiating an attempt, reset the backoff timer to its minimum value. This allows for an immediate retry if the conditions are right but still respects the backoff strategy to prevent overwhelming the network.\n\n### 4. Persisting Obligations and Partial Write Failure\n\nTo ensure obligations survive process death:\n\n- **Database Transactions**: Use database transactions when writing `DeliveryObligation` records to persistent storage. This ensures that either the entire record is written, or none of it is, preventing partial writes.\n- **Idempotent Writes**: When updating the state of a `DeliveryObligation`, ensure that the update operation is idempotent. This means that applying the same update multiple times has the same effect as applying it once.\n\nIn case of a partial write failure, the system should be designed to recover by either:\n\n- **Rolling Back**: If a write operation fails, roll back to the previous known good state.\n- **Retry Mechanism**: Implement a retry mechanism with a limited number of retries for the write operation itself, ensuring that a transient failure does not lead to data loss.\n\n### 5. Mobile Device Considerations\n\nGiven a mobile device with Doze:\n\n- **Event-Driven Triggers**: While capping the backoff at 1 hour may be appropriate for some scenarios, for mobile devices with Doze (a mode that restricts network access for apps), it might be more efficient to rely purely on event-driven triggers (like network connectivity changes, app wake-ups, etc.) to initiate attempts. However, a timer floor (much longer, e.g., a day) should still be considered for cases where events do not occur frequently enough to ensure timely delivery attempts.\n\n### 6. User-Visible States\n\nTo prevent the UI from claiming \"corrupted\" or \"failed\" for merely undelivered messages:\n\n- **Separate States**: Implement separate UI states for \"delivered,\" \"undelivered,\" \"failed\" (with a specific reason, e.g., protocol rejection), and \"corrupted\" (only for undecodable payloads).\n- **Progress Indication**: For undelivered messages, indicate progress or the fact that delivery is being attempted (e.g., \"sending...\") to manage user expectations.\n- **Explicit User Action**: Allow users to explicitly cancel or mark as \"failed\" a message if they choose to, providing them with control over the delivery obligation.\n\nThis approach ensures that the UI accurately reflects the state of message delivery, preventing false \"corrupted\" or \"failed\" states for messages that are simply undelivered but still have an outstanding delivery obligation.",
      "finish_reason": "stop",
      "cost": 0.0003956238,
      "truncated": false
    }
  ],
  "judge_model": "qwen/qwen3-235b-a22b-2507",
  "judge_synthesis": "**Synthesis & Recommendation (147 words):**\n\nAll models agree on core principles: enforce **storage quotas at enqueue** to prevent unbounded growth, **persist delivery obligations transactionally**, and avoid marking undelivered messages as \"failed\" or \"corrupted.\" They also concur on **receiver-side deduplication** using message IDs and **serializing retry triggers** to prevent races.\n\nThey **disagree** on retry policy post-transport-ack: Qwen and DeepSeek recommend pausing retries and escalating to user intervention; Llama suggests indefinite retries with idempotent handling. On backoff, Qwen opposes a 1-hour cap due to Doze; DeepSeek and Llama recommend longer caps (24h) or event-driven retries.\n\n**Final Verdict**: Enforce **pre-acceptance quotas**, use **atomic persistence**, **serialize triggers**, and adopt **event-driven retries** with a 24-hour fallback. Show \u201cSent, awaiting receipt\u201d in UI. Retain obligations indefinitely but **do not retry endlessly**\u2014escalate after long delays. Use **dedup IDs** and **receiver-side idempotence**.",
  "estimated_worst_case_cost": 0.0036328,
  "actual_cost": 0.0025656147,
  "max_cost_ceiling": 0.05
}