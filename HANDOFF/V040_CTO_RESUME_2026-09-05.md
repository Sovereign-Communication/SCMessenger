# V040 CTO resume — full context for a fresh CTO session (2026-09-05)

Status: Active (supersedes the 2026-09-01 resume block in CTO_STATE.md for
V040-endgame state; CTO_STATE.md deep history still valid for older context)
Entry: `/CTO` command loads this file first. Written by the CEO seat after a
full state re-verification at 2026-09-05 18:40Z (heads pulled live from origin).

## Mission

Get the 3-node test passing with FULL functionality on the true final tree,
then the CEO decides the v0.4.0 tag. Certification gate (from the confirmed
RCA): **a Windows CLI -> Pixel Text displays ON-DEVICE, plus a relayed hop via
the AWS node** — receipt-only delivery is NOT the gate. Mesh-up alone is not
the gate. Everything below is sequenced to that.

## Verified tree state (live from origin, 2026-09-05 ~18:40Z)

| Ref | Value |
|---|---|
| origin/main | 45ab59f9 |
| cto/v040-candidate-2026-09-02 | e97c3f82 (PR #272 head, unchanged) |
| PR #272 | OPEN, CLEAN, CI fully green at e97c3f82 (0 failed) |
| PR #276 | OPEN, CLEAN, head c7524f2b, CI 7/7 green (Android APK/JVM/Wiring, iOS x2, macOS, label) |
| #276 head commit | c7524f2b 2026-09-05 01:20Z "Centralize reconnect registration and outbox flushing" (swarm.rs +35/-62) |

## PR #276 state — the ONE open code gate

Fix for the CONFIRMED Android inbound drop-hop (Windows outbox ->
transport-manager registry never populated in production; `transport_handoff`
count 0 for the whole session; every flush returned PeerNotFound forever).
RCA: tmp/run-evidence/aws-rebuild-e97c3f82-20260904/rca/RCA-CONFIRMED-DROP-HOP-2026-09-04-2020Z.md

Review iteration R1-R8 complete, all REQUEST_CHANGES so far:
- R1 filed: HANDOFF/review/V040_OUTBOX_FIX_REVIEW_QWEN_2026-09-04.md (A1-A6).
- R2-R7: fixes/dispositions landed per round (commits 0239a6f9, 97e9f62c,
  1b40122e, 11f24fa9, 27ffd6b0, 4ed08d5c, f5ee9c13). Raw: tmp/rev276_r2..r7_*.
- **R8 reviewed the final head c7524f2b and returned REQUEST_CHANGES — NOT yet
  filed into HANDOFF/review/ (raw: tmp/rev276_r8_response.md, brief:
  tmp/rev276_r8_brief.md). Findings:**
  - F1 Medium: `flushed_this_connection` is per-PEER; partial close then
    reconnect on a second path never flushes the outbox (set entry survives
    because only the final-close arm removes it).
  - F2 Low: identify-site `connection_tracker` guard can skip registration when
    identify precedes ConnectionEstablished; fix = fall back to
    `swarm.is_connected()` as liveness proof.
  - F3 Low: `set_swarm_peer_connection` takes the read lock 3x; tick() can
    interleave between the calls.
  - F4 Low: wasm32 event loop never calls register_and_flush / set_swarm_peer
    (predates PR; helper made it visible).

### Next steps, in order (the critical path)

1. File the R8 verdict into HANDOFF/review/ and update the dispatch file
   (HANDOFF/freebuff/queue/V040_REVIEW_DISPATCH_276_OUTBOX_FIX_QWEN_2026-09-04.md
   still says R1 COMPLETE — stale).
2. Disposition F1-F4 against the tree WITH EVIDENCE. F1 is a real edge — fix
   (per-connection key or clear-on-fresh-reconnect). F2 apply the fallback.
   F3 batch or document. F4 wire the wasm arm or document the boundary plainly.
   No waiving (CEO directive on file). If a fix commit moves the head: CI must
   re-run to green before re-dispatch.
3. qwen R9 (same continuous non-author identity, read-only, no posting) until
   a plain `Verdict: APPROVE`. Only APPROVE closes the Rule-8 gate.
4. Merge #276 into cto/v040-candidate-2026-09-02 (per-merge gates: tip MATCH,
   git diff --quiet origin/main...branch, git cherry, merge-tree exit 0 on
   swarm.rs/iron_core.rs; log to HANDOFF/freebuff/inbox/V040_MERGE_EXECUTION_LOG_2026-09-03.md).
5. Re-pin the #272 verdicts at the NEW final head (they are pinned to e97c3f82
   and do NOT cover a post-#276 head): FINAL APPROVE delta + FLAG-5 deferral,
   via the harness lane (GO, per below). Rule-8 resolution APPROVE also needs
   a delta addendum if the resolution files changed.
6. Rebuild wincli + APK at the final head via CI artifacts, stage per-SHA under
   .codebuff_deploy/, verify provenance (merge-ref tree == head tree), clean
   reinstall on the Pixel (CI keystore; expect uninstall-first).
7. Full 3-node validation on the fixed tree (all three nodes same SHA) per
   HANDOFF/freebuff/inbox/V040_MERGE_READINESS_RUNBOOK_2026-09-04.md Section B
   + HANDOFF/freebuff/queue/V040_3NODE_VALIDATION_RUNBOOK_2026-09-03.md:
   SEED-DIAL, /api/peers all-three, external-address diagnostics, relay
   custody/audit, coordinated restart, and the certification gate (on-device
   Text + AWS-relayed hop). Include the secondary RCA finding: contact
   canonicalization (Windows keys Lucas as wire-peer canonical 176109b9; phone
   chat identity b9fe29c5) — disposition it in the loop or a follow-up PR; if
   it blocks delivered-Text, it is in scope and required.
8. Report to CEO -> merge #272 -> CEO tag decision (not the CTO's).

## Live rig (verified 2026-09-04, rebuild DONE 12:40Z)

| Node | Identity | State |
|---|---|---|
| AWS (new) i-0b41aab756eabd514 | 12D3KooWGvCWJ... @ 3.91.5.1:9001 | image sha-e97c3f8 (docker-publish run 33860486330), ec2-user docker group baked in |
| AWS (old) i-0b735c4f26aea42ed | 12D3KooW9uRMQT... @ 54.235.20.24 | STOPPED, volume retained = rollback layer. Termination is a separate explicit CEO go AFTER post-validation green. Do NOT terminate on this approval. |
| Windows CLI | 12D3KooWD6vZQ... | re-seeded to 3.91.5.1; log .codebuff_deploy/windows/wincli-e97c3f82-20260904T021737.log |
| Pixel 6a | 12D3KooWBPdNE... (chat id b9fe29c5..., "Lucas") | app data preserved through instrumented-RCA restore; CEO drives this leg |

Same-SHA 3-node mesh PASS was recorded 12:40Z (tmp/run-evidence/aws-rebuild-e97c3f82-20260904/RUN-RECORD-post.md)
and dead-mark defect zeroed — but that was PRE-#276; it does not carry forward.
Evidence note: pixel data backup pre-instrumented.tar (md5 e09afb9c...) is the
restore provenance.

## Approvals and standing directives (CEO seat)

- APPROVAL: HANDOFF/freebuff/inbox/V040_CEO_APPROVAL_PUSH_276_TO_FULL_3NODE_GREEN_2026-09-04.md
  — push #276 through Rule-8 to plain APPROVE (no waiving), then the sequence
  in "Next steps" above.
- Harness lane: GO (user). Known defect on file:
  Harness/handoff/VERIFY_PANEL_SHORTFALL_CONVERGENCE_2026-09-04.md (panel
  shortfall misreported as disagreement; judge output degraded — deterministic
  tallies are the source of truth; 429s need backoff). Usable, read the caveat.
- #267 ordering: delegated to CEO seat (landed). #268/#270/#271/#269: merged
  (see V040_MERGE_EXECUTION_LOG). #274 (nimble-peer recycle): merged into the
  candidate line; Mobile CI rerun completed.
- Standing: no self-merge, no tag, no release; every merge individually logged;
  main head must be re-verified before and after each merge; stop-and-report on
  any head move vs the recorded value.

## Operational discipline (paid-for lessons)

- Local clock is HST (UTC-10). ALL cutoffs and comparisons use Z. `find
  -newermt 'HH:MM'` parses local — a 09:30 local cutoff read as 19:30Z and
  produced false "no CTO movement" reports on 2026-09-04.
- Never two build tools at once; Windows host is the authoritative build env.
- Shared checkout: stage explicit paths only; never revert files you did not
  create; dispatch into worktrees (tmp/cand-merge precedent).
- Rule-8: changes under core/src/{crypto,transport,routing,privacy} need a
  non-author APPROVE with evidence before merge. swarm.rs/observation.rs/
  ledger_entry.rs/iron_core.rs are the conflict-prone set.
- Read raw bytes when a file shows [BLOCKED] in the read tool — that has been
  a display artifact, not file state (verified twice).
