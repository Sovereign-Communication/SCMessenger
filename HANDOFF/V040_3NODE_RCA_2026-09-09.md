# V040 three-node RCA — all issues, all three nodes

Written by the CEO seat, 2026-09-09T01:35Z, from this session's disk artifacts
and fresh commands. Every item cites the evidence it came from. Status values:
FIXED (code landed, commit cited) / ACTION (fix exists, execution pending) /
OPEN (fix not yet implemented) / PROCESS (campaign discipline fix, no code).

Supersedes nothing; complements the checkpoints. Feeds
`HANDOFF/V040_CTO_NEXTRUN_PACKAGE_2026-09-09.md`, whose entry gates reference
the fix IDs below.

## Node 1 — Windows CLI

| ID | Issue | Root cause | Status | Evidence |
|---|---|---|---|---|
| W1 | P0: node advertised an ephemeral/NAT-observed port as its external address (peers learned unusable endpoints; a `:443` artifact appeared in diagnostics) | Observation consensus ranked a NAT-mangled peer observation above the real endpoint; nothing let the operator pin the truth | FIXED live — configured-external-address primacy (`external_addr` config knob -> `AddressObserver::set_configured_external` -> `SwarmCommand::SetConfiguredExternalAddress`, wired in `cmd_start`/`cmd_relay`), commit `74253491`; live diagnostics show `external_addrs == ["147.81.41.188:9001"]` exactly | T14 checkpoints 232000Z + GOLIVE 001500Z; CEO live curl 2026-09-09T01:29Z |
| W2 | AWS bootstrap link depends on the `SC_BOOTSTRAP_NODES` env var; a restart without it boots healthy but peerless (observed once at go-live) | `bootstrap.rs::default_bootstrap_nodes` reads the env before build-time defaults; `config.json bootstrap_nodes` is empty, so config alone carries nothing | OPEN — smallest fix: persist the AWS multiaddr into `config.json bootstrap_nodes` (then verify a no-env restart keeps the AWS peer). Launcher `tmp/cto/T14_GOLIVE/launch_node_env.ps1` is the interim sanctioned relauncher | GOLIVE checkpoint section 2, "WARNING" |
| W3 | Three different Windows binary hashes confused the campaign record (`d2f75243` w/ recorded `94cfe7d` provenance; `1a736ac9` exact-candidate build; `829efe2c` current live ba474a7a build) | Each build tree produced its own exe; no single provenance ledger; the "recorded artifact" was never reconciled | PROCESS — next run defines ONE candidate (one commit, one build, one hash per node) recorded in the checkpoint at PREFLIGHT; rollback artifacts live OUTSIDE reclaimable `target/` trees | 171520Z checkpoint artifact-hashes section; GOLIVE section 1 |
| W4 | Rollback binary (`1a736ac9`) was deleted by the disk-reclaim (it sat inside `cto-win-build-85cb4c67/target`) | Rollback artifact stored in a regenerable build cache tree | OPEN (operational) — surviving rollback paths: `tmp/radio-85cb4c67/scmessenger-cli-85cb4c67.exe` (d2f75243, re-verified) and a rebuild from pushed `85cb4c67`; before the next cutover, stage the new rollback binary outside any `target/` | CEO reclaim log in `HANDOFF/CEO_STATE.md` 2026-09-09T01:4xZ |
| W5 | Windows LE advertisement success never proven; log explicitly says btleplug peripheral advertising is not enabled (WinRT GATT provider path start marker exists only) | Advertiser START markers were treated as readiness in early checkpoints; no advertisement-confirmation capture exists | OPEN — next run must capture advertisement-confirmation (or the Pixel observing Windows' SCM beacon), not a start marker | `tmp/cto/V040_LIVE_RELAY_BLE_DIAG_20260908T203506Z.txt` lines 128-147 |
| W6 | Pixel repeatedly failed to dial the Windows LAN peer `192.168.0.222:9001/9002` (IO error / Network error) while mDNS discovery of Windows worked | UNVERIFIED Windows firewall inbound hypothesis (CTO check-in 22:50Z); bind addresses are 0.0.0.0 so listeners themselves are global | OPEN — needs a live inbound test; if firewall blocks LAN inbound, operator adds an allowlist rule for 9001/9002 (ruling required: firewall changes were barred this session) | FOCUSED logcat 205239Z `MeshRepository$ensureTransportManager` failures 10:51:44; CEO_STATE CTO check-in 22:50Z |

## Node 2 — AWS cloud node

| ID | Issue | Root cause | Status | Evidence |
|---|---|---|---|---|
| A1 | Version skew: AWS runs `85cb4c67` while Windows runs `ba474a7a` (T14 + BLE-01 tree) | AWS was redeployed at the candidate before the fixes existed; no redeploy since | ACTION — redeploy AWS at the frozen next-run commit (tracked scripts: `.codebuff_deploy/aws/`) so all three nodes certify on one tree | CEO live curl 2026-09-09T01:29Z (both `/version` outputs) |
| A2 | AWS binary hash never collected (UNVERIFIED in every checkpoint) | The read-only HTTP/EC2 owner checks cannot see the binary; no SSH step was included | OPEN — add an SSH `sha256sum` (and `docker image` digest) capture to the next package's PREFLIGHT | 171520Z + 174811Z checkpoints ("AWS executable hash ... UNVERIFIED") |
| A3 | Two EC2 instances share the `scm-always-on-node` Name tag (i-0b735c4f stopped, i-0b41aab7 running) | Tag reuse; inventory filter matches both | OPEN — operator: retire or rename the stopped instance so deploy tooling cannot target the wrong one | 174811Z checkpoint, fresh AWS result section |

## Node 3 — Pixel 6a

| ID | Issue | Root cause | Status | Evidence |
|---|---|---|---|---|
| P1 | BLE discovery died after the first duty-cycle window (`isScanning` stranded true, scheduled restart never fired; live logcat showed "BLE scan window ended" then `peersDiscovered=0` forever) | `stopScanningInternal()` left the flag set on every path | FIXED, NOT INSTALLED — `BleScanner.kt` clears the flag in all paths + regression test; commit `74253491`; APK `2f07ed91` staged (`android/app/build/outputs/apk/debug/app-debug.apk`); operator installs it as the next run's Phase 1 action | BLE01 checkpoint 224800Z; BleScannerTest PASSED (gate log) |
| P2 | Pixel source provenance rests on APK-hash-equality only | Installed APK was never traceable to a specific tree | ACTION — installing the staged BLE-01 APK closes this: it is built from the committed tree (`ba474a7a`-family); record install + `pm path` hash in the next PREFLIGHT | GOLIVE section 4; preflight provenance blockers |
| P3 | Phases 3-5 never executed (radio isolation, single probe, correlation) | Campaign consumed by T14/BLE-01 repair + go-live before reaching them; overall verdict BLOCKED | OPEN — these are the actual test; the next run is planned around them | All checkpoints' verdict tables |

## Cross-cutting

| ID | Issue | Root cause | Status | Evidence |
|---|---|---|---|---|
| X1 | Rule-8 adversarial review outstanding for `core/src/transport` changes (`0a33c009` listen-port allowlist; `74253491` configured-external-address) | Both diffs are in the gated perimeter; the CTO authored and cannot self-approve | OPEN — MERGE GATE; dispatch to an independent reviewer before any merge to main (live-node deployment is not a merge and was correctly treated as such) | CTO check-ins 23:25Z; AGENTS rule 8 |
| X2 | Full core regression suite UNVERIFIED — the disk-full event killed cargo mid-build (crate-not-found + ICE in the logs); the check-in over-claimed it as PASS | Disk hit 100% during the run | ACTION — rerun now that 33 GB is free; next package gates on a green suite with pasted `test result:` lines | `tmp/cto/REGRESSION_20260909T002337Z/` logs; CEO audit 2026-09-09T01:20Z |
| X3 | Targeted gates were green while the full suite was broken | The suite and the targeted gates ran at different times against different disk states | PROCESS — next package requires BOTH: targeted gates AND the full suite, with raw logs preserved | T14_GATE_1..4 logs (6/6, 1/1, 4/4) vs REGRESSION logs |
| X4 | `tmp/cto/` evidence (go-live captures, launcher, gate logs) is gitignored and disk-only | `tmp/` excluded by design; preservation was never decided | OPEN (operator decision) — archive selected evidence under `HANDOFF/` or accept disk-only lifetime | CEO_STATE 2026-09-09T01:3xZ push note |
| X5 | Disk-full event itself | 26 GB main `target/` + 11 GB worktree targets + 13 GB tmp on a 237 GB SSD with many worktrees | MITIGATED — 36.76 GB reclaimed (sanctioned script + scoped cand-merge target delete); standing practice: run `scripts/reclaim_safe.py` before builds; keep rollback artifacts outside `target/` | CEO_STATE reclaim log |
| X6 | No final `v0.4.0` tag; same-candidate certification impossible across nodes without one tree | Release sequencing not yet reached | OPEN — after X1/X2 close and the next run certifies, tag per the ship plan | GOLIVE provenance section; earlier checkpoints' tag listings |

## Parity verdict at RCA time (2026-09-09T01:29Z, fresh commands)

- Windows: running, healthy, `ba474a7a`, T14 live, 1 peer (AWS), outbox 0 — the reference tree.
- AWS: healthy but `85cb4c67` — one tree behind (A1).
- Pixel: `0.4.0` vc14, APK `5090a835` installed — does NOT yet contain BLE-01 (P1 fix staged, not installed).
- Parity is therefore NOT yet achieved: the three nodes run three different artifact generations. Closing A1 + P1 (plus X1/X2 as gates) is what makes a same-candidate three-node test meaningful.
