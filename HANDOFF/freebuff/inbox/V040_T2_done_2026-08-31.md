Task: V040_T2_UNIFY_PEER_LEDGER_STORES.md
Type: DONE
PR: https://github.com/Sovereign-Communication/SCMessenger/pull/262
Date: 2026-08-31

## Gates -- all run on the Windows host, in the isolated worktree
`scm-t2-unify-ledgers` (@69a8ba57, branch `freebuff/v040-t2-unify-peer-ledgers`)

- `cargo clippy --workspace --all-features -- -D warnings` -> PASS
- `cargo test --workspace --no-run` -> PASS (all test targets compile)
- `cargo fmt --check` -> exit 0
- Tests (executed): core lib 1395/1395; CLI lib 77/77; 40+ integration
  binaries all green, incl. the rewritten adversarial gate (6),
  mesh acceptance (3), ledger_seeding_hardening (6), dial_policy (16).
  0 failures anywhere.

## What shipped

1. The CLI's persisted `peers.json` is DELETED as a store. `ConnectionLedger`
   is now process-lifetime dial state only, over a handle to the core
   `LedgerManager` (same storage-path registry entry as IronCore's).
2. One-time migration imports surviving peers.json entries into the core
   store preserving `locally_verified`, archives the file, runs once.
   All 7 `save()` call sites removed.
3. Core `LedgerEntry` gained `locally_verified`/`is_bootstrap`/`first_seen`/
   `observed_peer_ids`/`label`; only `record_connection` (a completed outbound
   dial) verifies; disclosure and dial-candidate gates respect the rule.
4. Four real defects surfaced BY the rewritten tests and fixed in the port:
   rebind no longer observes the departing identity (P0 stale-identity
   guard restored), canonical-hex/base58 self-filter parity, operator
   bootstraps dialable pre-first-success, hearsay gate reinstated at
   ingress.

## Rule-8 -- NOT self-certified (flagged on PR #262)

Touches `core/src/{store,transport}`. Needs an adversarial APPROVE from a
reviewer who did not author the change before merge; merge is the native
seat's call, not mine. Do not treat the green gates as clearing Rule-8.

## UNVERIFIED (needs native seat)

- Android Kotlin compile: `MeshRepository.kt` updated to the regenerated
  binding shape (UDL dictionary order verified), but the Android gate is
  not runnable on this seat -- generated-sources are FFI-script produced
  (per the T10 ruling, they are absent in the worktree and a bindgen run
  could pass vacuously; I did not run it as a gate).
- Live fleet migration behavior: staged rollout recommended; unit suite is
  the acceptance here.

## Also flagged for the queue

T1 Half-2 branch is parked and rebases post-merge. Before T1's FFI/Android
gates run, the worktree must be built first so bindings regenerate (T10
ruling applies to scm-t1-boot-seed-dial/core/target/generated-sources,
which I confirmed is absent there).