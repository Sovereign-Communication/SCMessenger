# V040-T8 -- PR #271 open (WS11 DiagnosticsBundleFormatterTest restored)

Status: PR FILED -- awaiting review; gradle unit-task verification deferred to CI (host disk constraint)

## What

- Restored `DiagnosticsBundleFormatterTest.kt` (58 lines, UNMODIFIED) at its
  original path, recovered from the deletion parent `149d3725^`. The removal
  premise ("class deleted by iterations") was false -- the class exists and is
  consumed by DiagnosticsScreen.kt. Verified the current DiagnosticsBundleInput
  fields and format() section markers are identical to what the test feeds and
  asserts, so no assertion changed.
- `docs/V0.2.0_RESIDUAL_RISK_REGISTER.md`: the REMOVED/UNVERIFIED note from
  #260 is replaced with the live link + RESOLVED evidence note.
- `bash scripts/docs_sync_check.sh`: PASS with the live link (local).

## Honest verification note

The Android unit-test task (`./gradlew :app:testDebugUnitTest`, per mobile.yml)
could not run on this host: the app module unconditionally runs a cargo-ndk
cross-compile (no skip flag in app/build.gradle) needing a fresh multi-GB
target tree; the host had 5.5G free (98%) and the partial build was consuming
5.4G and climbing. Killed the build, reclaimed the artifacts (11G free after).
The gradle gate is UNVERIFIED on this host -- CI runs the exact task on a fresh
runner. This is flagged in the PR body, not glossed.

## Filed

- PR: https://github.com/Sovereign-Communication/SCMessenger/pull/271
- Commit: 8fc58817 on `freebuff/v040-t8-restore-test`
- Scope: test (+58) + register (-2/+2). No production code; no Rust gates apply.

Lane: #267/#268/#269/#270/#271 open, awaiting non-author adversarial reviews
(#270 P0 ephemeral-port, #271 T8). #264 (T12) open -- blocks T9. T6/T7/T11
parked per CEO. T10 resolved-on-main. T14-pre-existing closed by #269.- CI VERIFICATION 2026-09-01: run 33568298552, job "Android JVM Unit Tests" completed SUCCESS -- both restored tests PASSED on the fresh runner (bundle includes required tester sections / bundle preserves core diagnostics and logs). The gradle UNVERIFIED flag is cleared; PR body updated.

**2026-09-01 addendum:** all 30 checks on #271 passed on fresh runners (full matrix green, zero failures) - Android Debug APK, iOS / iOS Build / iOS Build & Simulator Test, Test (macos/ubuntu/windows), macOS Native Tests, all lint lanes, Bindings (Kotlin/Swift), WASM, FFI Surface Contract, Docs, Windows CLI Artifact, CodeQL. Android JVM Unit Tests (run 33568298552) executed and PASSED both restored test cases.
