# V040-T4 DONE -- routing engine fed on ConnectionEstablished (D6 unblocked)

Date: 2026-08-31 ~19:40
From: Freebuff lane
Type: DONE (PR open, merge-blocked)

## PR

- **#263** -- `freebuff/v040-t4-routing-feed` base `main`
  https://github.com/Sovereign-Communication/SCMessenger/pull/263
  Committed: `bc5bff0f` -- 3 files, +232/-6
  (`core/src/transport/swarm.rs`, `core/src/iron_core.rs`,
  `core/src/routing/local.rs`)

## What changed

`IronCore::routing_peer_seen` gained its first caller: both the native and
WASM `ConnectionEstablished` arms now feed the optimized routing engine
beside the existing ledger-convergence block (every path sighting feeds;
LocalCell accumulates transports, peer_seen clears the negative cache).
Added `TransportType::Circuit` so relayed-circuit endpoints record
distinctly from direct TCP (score rank: BLE < WiFi < Circuit < TCP <
QUIC); extended `parse_transport_type` for ws/wss/circuit (WS previously
fell through to BLE). Routing types are Rust-internal: no UDL/FFI change.

## Gates (Windows host, x86_64-pc-windows-msvc, Freebuff container)

- `cargo test --workspace --no-run`: PASS (all members incl. mobile)
- `cargo clippy --workspace --all-features -- -D warnings`: clean
- `cargo fmt --check`: clean
- `cargo test -p scmessenger-core --lib`: 1394 passed (4 new acceptance
  tests: confidence 0 -> non-zero, TCP vs circuit recorded distinctly,
  multiaddr derivation, parse contract)
- `cargo check -p scmessenger-wasm --target wasm32-unknown-unknown`:
  PASS -- this caught a real partial-move of `endpoint` in the WASM arm
  that no native gate would have found; fixed and re-verified.
- Acceptance 3 (live node confidence) is UNVERIFIED: deploy-seat task
  after merge.

## Review gate flag (Rule 8)

**REQUIRED: adversarial APPROVE** -- touches `core/src/transport/` and
reaches `core/src/routing/`. No self-merge; do not merge on green CI
alone.

## Related

PR #215 (draft routing_peer_seen) superseded by #239 + this task --
recommend closing it.

## Disk note

Mid-gate, C: hit 100% (LNK1318 symptom). Freed 35.6 GB via the sanctioned
`scripts/clean_target.sh --all` on the parked scm-t2-unify-ledgers
worktree target (my own worktree; T2 PR already filed). 37 GB free now.