Task: V040_T5_DOCS_SYNC_GATE_IS_RED.md
Type: DONE
PR: https://github.com/Sovereign-Communication/SCMessenger/pull/260
Date: 2026-08-31

## Evidence

- `bash scripts/docs_sync_check.sh > out.txt; rc=$?; tail -3 out.txt; echo EXIT=$rc`
  -> `docs-sync-check: PASS`, `EXIT=0` (was FAIL/exit 1 on clean main).
- Diff: 1 file, 4 insertions / 2 deletions
  (`docs/V0.2.0_RESIDUAL_RISK_REGISTER.md` only).

## What I found, beyond the filed premise

The filed premise said the linked test was "deleted with no surviving
equivalent" -- correct, and the deletion commit's own rationale was also
wrong. `149d3725` removed `DiagnosticsBundleFormatterTest.kt` with the message
"remove orphaned test (class deleted by iterations)", but the class it tested
was never deleted: `DiagnosticsBundleFormatter.kt` still exists and is consumed
by `DiagnosticsScreen.kt`. The test was not orphaned; coverage is genuinely
lost, not superseded.

## Disposition (per the task's evidence rule + item-4 ruling)

The register now states, in one sentence, that the test was deleted under a
false premise, that no test covers `format()`, and that the evidence item is
`UNVERIFIED` until the 58-line test is restored (survives at `149d3725^`). The
broken link was replaced with that note, not deleted to make the check pass.
No other rows changed.

## Not in this PR (flagged)

Restoring `DiagnosticsBundleFormatterTest.kt` from `149d3725^` is the real
repair -- the class it tests is unchanged, so it should compile as-is. It is an
Android test, so it belongs in a separate change that runs the Android build
gate. Recommend a follow-up ticket.
