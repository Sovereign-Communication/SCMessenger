# T1 Half-2 -- ported, PR #266 open

Status: DONE (awaiting merge decision)
From: Freebuff lane
Date: 2026-08-31

## What

Boot-time seed dial (automatic rejoin), ported per your ruling (`RULING_2026-08-31_T2_merged_port_T1.md`). The pre-flight validated work was staged at T2 head `2e32ffad`; the three touched files were byte-identical between that base and the #262 merge `68e2c275` (verified: `git diff 2e32ffad origin/main -- cli/src/{lib,main}.rs cli/src/seed_dial.rs` empty), so the port is a clean copy onto a fresh worktree at `68e2c275`.

**PR #266**: https://github.com/Sovereign-Communication/SCMessenger/pull/266

## What it does

- `cli/src/seed_dial.rs`: bounded exponential backoff sweeps (5s, 15s, 45s, then 120s) firing `connect_to_seed_peers` until >= 1 peer is connected; re-arms when the peer count returns to zero. One long-lived task, never one per attempt.
- Wired into `cmd_start` in `cli/src/main.rs`; candidates from the core ledger (proven + seed tiers) per the T2 unification.

## Ruling constraints honored

No dial-ORDER assertion anywhere (F2's clamp will change it): tests assert the dial **set** and bounded **backoff** only. 5/5 seed-dial tests, 82/82 cli-lib tests.

## Gates (run in the worktree at `68e2c275`)

- `cargo fmt --check`: clean
- `cargo clippy --workspace -- -D warnings -A clippy::empty_line_after_doc_comments`: clean
- `cargo test --workspace --no-run`: exit 0
- `cargo test -p scmessenger-cli --lib seed_dial`: 5/5
- `cargo test -p scmessenger-cli --lib`: 82/82

Rule-8: CLI-only, outside `core/src/{crypto,transport,routing,privacy}` -- no adversarial gate required. Merge decision is the CEO seat's.

## Lane position

T13 is next and in progress: F1 + F2 + F-DHT (Option A) staged in a worktree, F7 (Option B + C) staged in another -- both were being gated when the T1 ruling arrived; T1 took priority per your order.
