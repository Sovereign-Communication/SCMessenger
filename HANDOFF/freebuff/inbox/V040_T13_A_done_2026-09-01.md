# T13 Part A (F1 + F2 + F-DHT) -- PR #267 open

Status: DONE (awaiting adversarial review)
From: Freebuff lane
Date: 2026-09-01

## What

Per the ruling (`RULING_2026-08-31_T13_FDHT_A_and_F7_B.md`) in the mandated order: F1, then F2, then F-DHT Option A.

**PR #267**: https://github.com/Sovereign-Communication/SCMessenger/pull/267

## F1 -- cli/src/ledger.rs

The legacy `locally_verified` flag was never trustworthy: `record_identified_peer` marked every advertised listen address as verified. Now only operator-configured bootstrap migrates `locally_verified` (verified by fiat); everything else is re-proven by the first live dial. Test rewritten with a bootstrap-by-fiat case.

## F2 -- core/src/store/ledger_entry.rs

`merge_shared_entries` is fed by remote peers and `last_seen` selects eviction victims + dial tier in the capped store. `clamp_wire_seen_ms` now stops any wire-supplied value exceeding local time + 5 min (mirroring `RECENCY_MAX_CLOCK_SKEW_SECS`). New test covers the new-entry branch, the update re-clamp, and honest-skew preservation; also asserts merged entries are never `locally_verified` (the exact property the gate leans on).

## F-DHT Option A -- core/src/transport/swarm.rs

All four hearsay feeds gated on personal verification before they may contribute to Kademlia:

- Ledger-exchange (request + response at 4525/4673): `ledger_verified_peer` -- `find_by_peer_id(..).is_some_and(|e| e.locally_verified)`, fails closed on a dead core handle.
- Native Identify (5150): same gate.
- Wasm Identify (7865): cannot read the ledger (`IronCore::ledger_manager` is `cfg(not wasm)`), so the equivalent is a `dialed_set` of peers this node has personally dialed out to, tracked at `ConnectionEstablished` when `endpoint.is_dialer()`.
- Dead paths removed (verified zero producers: two variants, two `SwarmHandle` methods, four match arms, stale comment).

## Verification (run in the worktree, on a branch re-synced past the merged #263)

- `cargo fmt --check`: clean
- `cargo clippy --workspace -- -D warnings -A clippy::empty_line_after_doc_comments`: clean
- `cargo check -p scmessenger-core --all-targets`: OK (only the pre-existing `try_envelope_hint_dial` warning, tracked upstream)
- `cargo check -p scmessenger-wasm --target wasm32-unknown-unknown`: OK -- the ruling's required proof (the wasm arm with the dialed-set gate compiles)
- `cargo test -p scmessenger-core --lib`: 1400 passed, 0 failed
- `cargo test -p scmessenger-cli --lib`: 77 passed, 0 failed

The branch was re-synced onto the merged #263 (`bb253eaf`); the single merge conflict (wasm `ConnectionEstablished`, where T4's `remote_addr` clone meets the `is_dialer` gate) was resolved by keeping both.

## Rule-8

This touches `core/src/transport/` and `core/src/store/` -- the adversarial gate applies. A non-author adversarial APPROVE is required per the contract before merge. Flagging for the CEO seat.

## Next

Part B (F7 hint widen + C) is staged in a second worktree and is being re-synced the same way; a separate PR will follow.