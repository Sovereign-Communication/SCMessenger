Type: DONE
Task: V040 CANDIDATE 272 re-review triage (round 2)
SHA: fc0f5ae0 reviewed -> 3891d11c fixed (pushed)
PR: #272 body carries the full round-2 triage table

qwen re-review (qwen3.8-2.4t-a95b, ledger 100%/1M) returned REQUEST_CHANGES with 6 findings.
All verified against the tree: 4 REAL (fixed), 1 SPLIT (parser arms fixed; wasm-feed claim was a
reviewer hunk-position error), 1 NOT-APPLICABLE (wasm has no promotion path).

Fixes in 3891d11c: relay-first multiaddr classification (circuit-over-WS -> relay), restored
circuit/relay parse arms + confidence regression test, TCP-only listen-port allowlist
(UDP/QUIC rejected), both deleted tests restored, deterministic eviction via the shared
comparator ordering.

Gates all PASS on 3891d11c: core --all-targets 0 errors; core lib 1400/0 (5 ignored);
cli lib 82/0; wasm32 check clean; rustfmt + diff --check clean.

Verdict record: HANDOFF/review/V040_CANDIDATE_272_REVIEW_QWEN_R2_2026-09-03.md
Next: non-author adversarial APPROVE of #272 at 3891d11c, then three-node validation on that SHA.
