# T13 Part B (F7 Option B + C) -- PR #268 open

Status: DONE (awaiting adversarial review)
From: Freebuff lane
Date: 2026-09-01

## What

Per the ruling (`RULING_2026-08-31_T13_FDHT_A_and_F7_B.md`): F7 Option B (widen routing hint `[u8;4]` -> `[u8;8]`) plus Option C (stable peer ordering).

**PR #268**: https://github.com/Sovereign-Communication/SCMessenger/pull/268

## Scope

- Hint widen across the routing surface: `engine.rs`, `global.rs`, `local.rs`, `multipath.rs`, `neighborhood.rs`, `optimized_engine.rs`, `resume_prefetch.rs`, `swarm.rs` hint generation, `iron_core.rs` (local hint + parse boundary), `cli/src/server.rs` RPC handlers.
- Option C: `peers_for_hint` returns reliable-first ordering (no more HashMap iteration order) so announcement/gossip selection is deterministic.
- Test fixtures updated to 8-byte width (`integration_mycorrhizal_routing.rs`, `integration_retry_lifecycle.rs`, `diagnostics.rs` tests, `iron_core.rs` unit tests).

## Out-of-scope boundaries (confirmed by tracing, stated in the PR)

- Drift envelope hint (`core/src/transport/drift/envelope.rs`) -- separate onion-routing protocol, not the routing-engine hint.
- wasm mesh store frames (`wasm_support` `messages_for_hint`) -- separate wasm mesh message store.
- BLE l2cap/gatt/beacon and discovery `node_shard` -- unrelated subsystems.

## Verification (worktree merged with latest main `67d19d3c` incl. T1)

- `cargo fmt --check`: clean
- `cargo clippy --workspace -- -D warnings -A clippy::empty_line_after_doc_comments`: clean
- `cargo check -p scmessenger-core --all-targets`: clean
- `cargo check -p scmessenger-wasm --target wasm32-unknown-unknown`: OK (CI-equivalent form)
- `cargo test -p scmessenger-core --lib`: 1400 passed, 0 failed
- `cargo test -p scmessenger-cli --lib`: 82 passed, 0 failed

The branch was re-synced twice during delivery (merged #263's T4, then #266's T1); both merges were clean (ort), with only the merge-reintroduced 4-byte fixtures needing completion -- all fixed and verified on the final merged state.

## Wire-format

Hint is now 8 bytes on the wire -- intentional per the ruling's sequencing (before the v0.4.0 tag, no installed base). No persisted state carries the old width; pure cutover, no migration.

## Rule-8

Touches `core/src/routing/` and `core/src/transport/` -- adversarial gate applies. A non-author APPROVE is required before merge. Flagging for the CEO seat.

## Lane position

T13 fully delivered (Part A #267 = F1+F2+F-DHT; Part B #268 = F7 B+C), both awaiting adversarial review. Remaining lane items: T10 (staged in `scm-t10-ffi-gate`, paused mid-acceptance), T9 (held until #264 merges), T6/T7.
- ADDENDUM 2026-09-01 (post-submission adversarial self-audit, per user directive): attacked #268 before re-review -- wrong-width writers, residual 4-byte artifacts, drift/wasm-mesh cross-surface leakage, parse boundaries, ordering determinism, persisted-state skew. Verdict: no code defect; no code changed (diff byte-identical to pushed head, 14 F7 files). Two doc corrections posted to the PR body: stale drift path citation (module is core/src/drift/, not core/src/transport/drift/) and an explicit version-skew contract statement (non-8-byte hints from pre-upgrade peers are dropped at ingestion / fail-closed at RPC, no corruption).
