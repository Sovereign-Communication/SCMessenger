# V040 CEO -> CTO sync note -- 2026-09-03

Task: V040_CEO_CTO_SYNC_2026-09-03.md
Type: SYNC / STANDING DIRECTIVE

SUPERSEDES `V040_CEO_RELAUNCH_2026-09-03.md` for context purposes. Treat this
note as the current standing instruction; the earlier relaunch prompt is
obsolete (its candidate-SHA step is complete -- see item 1). The evidence
source for every claim below is
`HANDOFF/freebuff/queue/V040_WORKTREE_RECOVERY_DISPOSITION_2026-09-03.md`
(sections A-D and the addendum G.1-G.9). Re-read the disposition before
acting; do not cross-reference other inbox files for facts.

## 1. Verified recovery state (do not re-derive)

- Registry: **9 worktrees remain** (main SCMessenger, scm-mailbox,
  scm-secutils, scm-t1-boot-seed-dial, scm-t1-half2-validation,
  scm-t10-ffi-gate, scm-t13-fdht, scm-v040-candidate,
  .claude/worktrees/scmessenger-freebuff-api-reset-fdee09) -- G.1.
- **10 worktrees removed, all recoverable**: 4 patch-merged into main
  (t1-half2, t2-unify-ledgers, t4-routing-feed, t5-docs-sync), 6 tips live
  on origin (t12-ci-pacing ec177fd9, t13-f7 7bafe83d, t13-fdht-gate
  80197ef5, t14-ephemeral 6fd0230b, t14-preexisting b2a7b345, t8-restore
  8fc58817); every one of the 10 local branch refs survives, so any worktree
  can be re-created with `git worktree add` + checkout. Nothing lost -- G.1.
- **All 29 recovery artifacts hash-verified byte-faithful** to current
  on-disk state, under repo-local `tmp/recovery/` with
  `tmp/recovery/MANIFEST.sha256` -- G.3.
- **4 backup refs protect the orphan commits**:
  `refs/backup/prunable-merge234-c1c99a8e` -> c1c99a8e35aa,
  `refs/backup/prunable-diff-repair-8de89136` -> 8de891363ddf,
  `refs/backup/prunable-cto-l7-audit-2fc9cf66` -> 2fc9cf6640d7,
  `refs/backup/prunable-cto-l8-kernel-d9403708` -> d9403708a2e3.
  Additional pre-existing cto-l7/cto-l8 refs double-cover the last two, and
  8de89136 is also a live branch HEAD -- G.4.
- **Architecture candidate committed and pushed**: scm-v040-candidate is
  clean at a759e0c70f61 = `origin/cto/v040-candidate-2026-09-02`; committed
  arch files match the `tmp/recovery/candidate-arch-variant/` export
  (cli/Cargo.toml differs only CRLF/LF) -- G.2.

## 2. Per-worktree clearing recommendation (six dirty worktrees)

All six remain registered and untouched. Clearing = `git worktree remove`
(never `rm -rf`), after any pending review below. Recovery artifacts for all
six are already exported and hash-verified (item 1).

| Worktree | Recommendation | Basis |
|---|---|---|
| scm-mailbox | **HOLD -- CEO review pending** | 193 modified files incl. binary `docs/NARC_MASTER_TRACKER.md` (export `50e8d82d0252`); committed state recoverable from `origin/docs/cto-dispatch-plan-20260821-auditor` (G.8.1) |
| scm-secutils | **DISCARD** | 16 files, proven purely CRLF/whitespace (diff exit 0); delta discardable after export (Section A) |
| scm-t1-boot-seed-dial | **DISCARD** | 2 modified files + UNIQUE untracked `cli/src/seed_dial.rs` `1b91722aaa42`, committed nowhere -- highest-value artifact, preserved (Section A); committed content merged (PR 258) |
| scm-t1-half2-validation | **DISCARD** | 2 modified files + untracked `cli/src/seed_dial.rs` `001086d7201e` exported; branch superseded by T2 commit (Section A, D) |
| scm-t10-ffi-gate | **DISCARD** | `scripts/ffi_surface.sh` delta exported (`ae579bc8a138`); committed content merged (PR 261) |
| scm-t13-fdht | **DISCARD -- do NOT cherry-pick** | G.9 verdict: delta fully superseded by `origin/freebuff/v040-t13-fdht-gate` 80197ef5; every novel hunk is an older/weaker iteration the gate branch revised to be stricter (pair-level DHT gate, no-skew clamp + F6, wasm insert-nothing). Cherry-picking would REGRESS gate policy. Branch local-only, upstream deleted |

## 3. Dependency-ordered merge sequence (genuinely unmerged)

All six PRs merge from their LIVE origin branches (SHAs above), not worktrees.
Every merge is individually CEO-approved; no self-merge, no tag, no release.
Each transport/routing/crypto merge requires a non-author Rule-8 APPROVE on
file with evidence attached BEFORE the merge request.

1. **T12 CI pacing** -- ec177fd9; CI workflows only; no Rule-8.
2. **T8 restore test** -- 8fc58817; Android test + doc.
3. **T13-F7 hint widen** -- 7bafe83d; routing; Rule-8.
4. **T14 preexisting-fixes** -- b2a7b345; transport; Rule-8.
5. **T14 ephemeral-port** -- 6fd0230b; transport; Rule-8.
6. **T13-FDHT gate** -- 80197ef5; transport/crypto; Rule-8 -- REVISIT the
   2026-09-01 ruling reversal before merge.
7. **Architecture candidate** -- a759e0c7 (`cto/v040-candidate-2026-09-02`);
   transport/routing + cli; FRESH non-author APPROVE REQUIRED; merge LAST,
   after same-SHA three-node validation passes.

Conflict-prone files (run a `git merge-tree` conflict check per pair before
each merge): `core/src/transport/swarm.rs` (T13-FDHT, T14-ephemeral,
T14-preexisting, architecture), `core/src/transport/observation.rs`
(T14-ephemeral, architecture), `core/src/store/ledger_entry.rs` (T13-FDHT
F2/F6 clamp work; ledger-unify heritage). See Section E.3.

Also close with evidence (cherry `-` / 3-dot exit 0) the already-merged PRs:
266 (T1 half2), 258, 261, and the T4/T5/T2 PRs; close-without-merge
`t1-half2-validation` (superseded) and the `docs/cto-dispatch-plan` PR
(planning doc, keep branch) -- Section E.1.

## 4. Governance reminders

- **Further worktree removal REQUIRES a CEO buyoff note on file first.**
  The 2026-09-03 execution removed 10 worktrees beyond the CTO's own
  6-worktree safe-clear proposal and no buyoff was on file (G.6). Do not
  repeat: propose, get buyoff, then execute per path.
- **No disk cleanup while cargo/rustc builds are live.** `scripts/clean_target.sh`
  refuses during builds; check `tasklist` first (G.7).
- **No self-merge, no tag, no release.** Every merge individually
  CEO-approved (Section E.4).
- Shared checkout: touch only your task files; never broad `git commit -a`,
  `git clean`, `git reset`, or `rm -rf` (AGENTS.md).

## 5. Return contract (per HANDOFF/freebuff/inbox/README.md)

- One file per message, named `V040_<what>_<YYYY-MM-DD>.md`, starting with
  `Task: <id>` and `Type: QUESTION | BLOCKED | PREMISE-WRONG | DONE`.
- Carry evidence, not impressions: the exact command and its raw output, a
  run URL, or the word `UNVERIFIED`. A claim that a gate passed is not the
  gate's output.
- Completion note: the PR number, what changed, and any acceptance criterion
  you could NOT satisfy.
- For each merge request, attach the per-PR evidence bundle: raw gate output,
  squash-merge proof (`git diff --quiet origin/main...<branch>` AND
  `git cherry -v origin/main <tip>`), and the Rule-8 approval artifact
  (reviewer identity, verdict file path, non-author statement).
- Blocked: what you tried, the exact command + output, and what unblocks you.

Evidence source: `HANDOFF/freebuff/queue/V040_WORKTREE_RECOVERY_DISPOSITION_2026-09-03.md`
-- Sections A-D and addendum G.1-G.9. If a fact here conflicts with the
disposition, the disposition wins; flag the discrepancy in your next note.

## 6. Addendum 2026-09-03 (verified by CEO audit pass)

- **Candidate advanced through two qwen review rounds.** The SHA in item 3.7
  (a759e0c7) is superseded: a759e0c7 -> fc0f5ae0 (R1 triage: routing feed
  restored, circuit/ws transport mapping, total_cmp) -> **3891d11c** (R2
  triage: relay-first classification, TCP-only allowlist, deterministic
  eviction). Candidate worktree clean at 3891d11c = `origin/cto/v040-
  candidate-2026-09-02` = `refs/pull/272/head`. Use **3891d11c** for
  three-node validation and the #272 merge. Verdict records:
  `HANDOFF/review/V040_CANDIDATE_272_REVIEW_QWEN_R2_2026-09-03.md` (and
  `..._2026-09-02.md` for R1).
- **PR #265 (`t12-docs-only-verify`, 6488a28b) -- close without merge.**
  Verified: its only content beyond #264 (ci-pacing ec177fd9, merge
  sequence item 1) is the throwaway doc `docs/t12_docs_only_verify_2026-
  08-31.md` (4 lines); the CI content (ffdeceb7) is identical to #264.
  Add it to the Section 3 close list.
- **#267 (#272 too) Rule-8 gate status:** the qwen R2 verdicts are
  REQUEST_CHANGES-with-fixes, not APPROVE. The CTO's triage notes correctly
  record "next: non-author adversarial APPROVE". For #267 the R2 recheck is
  a zero-diff "no defects found" disposition (that is the approve evidence);
  for #272 the R2 fixes were verified against the committed tree. CEO must
  either accept the verdict-file chain as the Rule-8 APPROVE evidence or
  dispatch one final APPROVE-only pass -- do NOT let #267/#272 merge with
  the gate recorded as "pending".
## 7. Addendum 2026-09-03 (three-node rollout readiness -- verified by CEO)

- **Rollout plan deliverable on file:**
  `HANDOFF/freebuff/inbox/V040_3NODE_ROLLOUT_PLAN_2026-09-03.md` -- the
  paste-ready execution plan for the three-node validation. GO-GATE (restated):
  qwen free-lane plain-APPROVE verdict files must exist for BOTH PRs
  (`HANDOFF/review/V040_CANDIDATE_272_FINAL_APPROVE_QWEN_2026-09-03.md`,
  `HANDOFF/review/V040_T13_FDHT_FINAL_APPROVE_QWEN_2026-09-03.md`), then
  re-verify the SHA at go-time: `git rev-parse origin/cto/v040-candidate-
  2026-09-02` must equal **3891d11c** and the scm-v040-candidate worktree must
  be clean. Nothing in the plan starts before that gate passes. The final
  APPROVE dispatch is `HANDOFF/freebuff/queue/V040_REVIEW_DISPATCH_267_272_
  FINAL_APPROVE_QWEN_2026-09-03.md` -- launch it when ready; do NOT re-dispatch.
- **AWS IAM GUARD -- the live node must be REUSED, NEVER terminated.**
  `.codebuff_deploy/aws/check.py` raw output (caller
  `scmessenger-relay-orchestrator`): `run_instances dry-run: DENIED`,
  `terminate_instances dry-run: ALLOWED`. A terminated node CANNOT be
  relaunched under the current policy, so `i-0b735c4f26aea42ed`
  (scm-always-on-node, t3.micro, public IP 54.235.20.24) is the only AWS node.
  Do NOT run `launch.py`, do NOT terminate. Redeploy = docker pull + container
  restart on the live instance. The work order's IP-churn sub-gate is
  UNVERIFIABLE until the IAM policy allows RunInstances -- record the dry-run
  DENIED output as the reason; re-mesh-without-churn remains testable.
- **Pixel/adb recovery state:** Pixel 6a ONLINE via wireless adb
  (`adb devices -l` -> `192.168.0.113:34123 device product:bluejay`, serial
  26261JEGR01896, Android 17). Endpoint is EPHEMERAL -- recovery is
  `adb mdns services` then `adb connect <ip:port>`; `android/install-clean.sh`
  already auto-recovers via the mDNS branch. No emulator on this machine
  (physical Pixel is the only Android path).
- **B2-B4 watch items (fresh-build steps in the rollout plan):** docker push
  target (`testbotz/scmessenger:latest`) not yet verified pushable;
  `.codebuff_deploy/wincli-3891d11c/` exe and the 3891d11c pixel APK do NOT
  exist yet -- both are build steps in the plan (steps 3-5), built from the
  candidate worktree at 3891d11c. Existing wincli/pixel artifacts are older
  SHAs and are NOT the validation build.

---

## 8. Addendum 2026-09-03 (late) -- GO-GATE PASSED, validation SHA is now 177bd840

1. FINAL APPROVE verdicts on file for BOTH PRs (Rule-8 chain complete, one
   continuous non-author qwen3.8-2.4t-a95b, ledger-recorded):
   - PR #272 @ 3891d11c APPROVE: HANDOFF/review/V040_CANDIDATE_272_FINAL_APPROVE_QWEN_2026-09-03.md
   - PR #267 @ 80197ef5 APPROVE: HANDOFF/review/V040_T13_FDHT_FINAL_APPROVE_QWEN_2026-09-03.md
2. SHA CORRECTION (supersedes section 7 pin): the CTO discovered #272's own CI
   was red at EVERY prior head (a759e0c7, fc0f5ae0, 3891d11c) -- the
   pre-existing core/tests integration test test_consensus_with_multiple_observations
   encoded the pre-allowlist contract. Fixed TEST-ONLY at 177bd840
   (set_listen_ports([1234, 5678])); code semantics unchanged from the
   APPROVE'd 3891d11c. The verdict file's delta addendum covers this move.
3. VERIFIED this pass (CEO/agent, 2026-09-03): 177bd840 vs 3891d11c diff is
   exactly 1 file (core/tests/test_address_observation.rs, +13/-8); the
   candidate worktree is clean at 177bd840; origin/cto/v040-candidate-2026-09-02
   = refs/pull/272/head = 177bd840; refs/pull/267/head = 80197ef5 unchanged.
4. CI on 177bd840 is green: 33/34 checks SUCCESS via gh (Test ubuntu/macos/
   windows, WASM, Cross Android x4, Windows CLI Artifact, Android Debug APK,
   FFI Surface Contract, lint/hygiene/CodeQL all green); only the iOS Build &
   Simulator Test leg was still IN_PROGRESS at check time (macOS Native Tests
   in that workflow already SUCCESS).
5. VALIDATION SHA FOR THE THREE-NODE RUN: **177bd840** (was 3891d11c).
   The rollout plan's GO-GATE (both APPROVE verdicts on file + SHA re-verify)
   is PASSED. Remaining execution per V040_3NODE_ROLLOUT_PLAN_2026-09-03.md:
   build wincli-177bd840 exe + 177bd840 APK + docker push, AWS node reuse
   (i-0b735c4f26aea42ed, IAM run_instances DENIED -- never terminate), Windows
   [SEED-DIAL] capture, Android install on the online Pixel 6a (ephemeral
   wireless adb, auto-recovered by android/install-clean.sh), then the three
   evidence gates.
