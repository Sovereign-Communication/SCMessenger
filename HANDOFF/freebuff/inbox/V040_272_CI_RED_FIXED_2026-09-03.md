# V040 #272 CI-red finding fixed -- validation SHA is now 177bd840

Task: V040_CEO_CTO_SYNC_2026-09-03.md (addendum 7 GO-GATE re-verify)
Type: DONE (with correction to the pinned SHA)

1. FINAL APPROVE verdicts filed for BOTH PRs: #272 @ 3891d11c APPROVE and
   #267 @ 80197ef5 APPROVE (paths per the dispatch brief). Rule-8 evidence
   chain complete: R1 -> R2 -> FINAL APPROVE, one continuous non-author
   (qwen3.8-2.4t-a95b), ledger-recorded.
2. CORRECTION: #272's own CI was red at EVERY head (a759e0c7, fc0f5ae0,
   3891d11c) -- test_consensus_with_multiple_observations failed on all 3 OS
   matrices. The local gate record (core 1400/0) ran the lib suite only; the
   core/tests integration suite CI runs was never executed locally.
3. Root cause: the unmodified main test encoded the pre-allowlist contract
   (AddressObserver with no listen ports expects consensus). The arch pass made
   the empty allowlist fail closed; the stale test dropped every observation.
4. Fix (177bd840, TEST-ONLY): the test declares the two ports under observation
   via set_listen_ports([1234, 5678]), mirroring swarm NewListenAddr wiring and
   the arch pass's own unit tests. Code semantics unchanged from the APPROVE'd
   3891d11c.
5. Verification (the gap closed): full `cargo test --workspace` at 177bd840 --
   1845 passed, 0 failed. fmt clean. Diff vs origin/main: the 7 arch files +
   this one test file.
6. Validation SHA for the three-node run: **177bd840** (was 3891d11c).
   Re-verify: git rev-parse origin/cto/v040-candidate-2026-09-02. CI is
   re-running on the new head; the verdict delta addendum is appended to
   HANDOFF/review/V040_CANDIDATE_272_FINAL_APPROVE_QWEN_2026-09-03.md.

7. CI CONFIRMED GREEN 2026-09-03: all 33 checks pass at 177bd840 (Test
   matrix macos/ubuntu/windows + macOS Native Tests + WASM + iOS + Android +
   all lint/analyze gates), zero pending, zero failed. PR #272 MERGEABLE.
