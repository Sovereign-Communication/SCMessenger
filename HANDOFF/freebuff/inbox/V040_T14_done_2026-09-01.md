# V040-T14 -- PR #269 open (two pre-existing Kademlia feeds closed)

Status: PR FILED -- awaiting adversarial review

## What

Closed the two ticket sites on current main head (67d19d3c) per the corrected-pair
doctrine (T13 F-DHT): an `add_address` lands only a pair OUR store proves;
unverifiable discovery feeds drop the add.

- mDNS Discovered (swarm.rs ~5019): unauthenticated LAN broadcast could bind any
  pid to any public addr and re-publish globally -- add dropped; the validated
  dial below is untouched (discovery/auto-connect unchanged), and a successful
  dial is the proof that later feeds the ledger-backed paths.
- DCUtR hole-punch (swarm.rs ~4895): was inserting OUR OWN
  `swarm.external_addresses()` under `remote_peer_id` (identity misattribution +
  leaking our addresses bound to a stranger's id). The DCUtR event carries no
  remote address, so nothing provable exists to insert -- add dropped; the
  hole-punched connection itself remains in use.

Residual behavior per site documented in the PR body.

## Sibling sweep

All NINE `add_address` sites on main re-enumerated and classified in the PR
(nine-site table): the two fixed here are the last unverified discovery feeds
not already assigned to #267 (ledger-exchange 4559/4707, Identify 5184, wasm
7919 are #267's gated sites; SwarmCommand arms 6542/6677/7178 have zero
in-tree issuers / operator-provisioned bootstrap surface -- cleared). No
`kademlia.add_address` outside swarm.rs in core/cli.

## Verification (worktree scm-t14-preexisting-fixes at 67d19d3c)

- fmt clean; clippy (documented gate) 0 errors
- core + cli --all-targets clean; wasm32 proof OK (scmessenger-wasm)
- core lib 1399/0; cli lib 82/0
- Diff vs origin/main: exactly `core/src/transport/swarm.rs` (+25/-18)

## Filed

- PR: https://github.com/Sovereign-Communication/SCMessenger/pull/269
- Commit: b2a7b345 on `freebuff/v040-t14-preexisting-fixes`
- Rule-8: adversarial review required (touches core/src/transport/) -- not
  folded into #267 per the CEO ruling; #267 remains its own review track.

Lane status: #267 rework + audit pushed, #268 open (audited, no defects), #269
open (T14). All three await non-author adversarial reviews.