# T12 -- CI concurrency groups + docs-only path skips: PR #264 open

Status: OPEN (PR filed, Rule-8 not required -- `.github/workflows` only, no
merge-blocked dirs)
From: Freebuff lane
Date: 2026-08-31
Re: `V040_T12_CI_CONCURRENCY_AND_PATH_FILTERS.md`

## Shipped

`freebuff/v040-t12-ci-pacing` (worktree `scm-t12-ci-pacing`), **PR #264**, 2
commits, 9 files (+174/-13):

1. **Concurrency groups on all 8 PR-triggered workflows.** New push cancels the
   superseded run on that ref; main-push runs keyed on the commit SHA and never
   cancelled (trunk-health record).
2. **cross.yml `paths-ignore`** for `**.md`, `HANDOFF/**`, `docs/**` -- the only
   non-required workflow without an existing path filter (mobile/ios/desktop
   already had `paths:`; Docker workflows are not PR-triggered).
3. **Required checks short-circuit in-job, never skipped (the trap).** Verified
   the four required contexts via the API (Repository Hygiene Checks, Lint,
   Rust Linting, Test (ubuntu-latest)). ci.yml + lint.yml now run a shared
   composite action `.github/actions/detect-docs-only` as step 1 and gate the
   expensive steps on its output; the job still starts and reports success in
   seconds. Hygiene runs its cheap git checks unconditionally (docs/rules
   changes agent behaviour; the docs-sync check lives in burndown_issues.sh,
   not CI -- nothing to protect).

## Verified live on CI

- **Acceptance 4:** two quick pushes to #264 -- first SHA's matrix shows
  `completed/cancelled`, second SHA's is the only one running. One matrix, not
  two.
- **Acceptance 3 direction:** a workflow change triggers the full matrix -- all
  25 checks on the head SHA.
- **Detection logic** exercised locally: docs-only = skip; code/mixed and
  critically workflow-only PR = run everything (a CI-fixing PR cannot skip its
  own checks).
- YAML parses for all 16 workflows + the composite action.

## UNVERIFIED -- acceptance 2 (docs-only PR) is post-merge

Attempted a throwaway docs-only PR (#265) based on this branch; it did not
exercise the new workflows -- GitHub evaluates workflow files from the merge
ref, and a non-main base fails the `branches: [main]` filter. A docs-only PR
against main cannot see these workflows until #264 merges. After merge, one
docs-only PR must show exactly the four required checks (fast) and skip Cross;
before/after check count goes in the PR body per acceptance 5. Flagging rather
than approximating, per the brief.

## Notes

- This PR's own full-matrix run is the first real exercise of the concurrency
  groups under queue load; Test/iOS/Windows jobs still pending at write time.
- Rule-8: not required (no core/src/{crypto,transport,routing,privacy}
  changes). A workflow YAML review by a non-authoring seat is still welcome
  given the trap's severity.
- No emojis; no `$?` after a pipe in the detection script (captured values).

Lane position: T12 filed; T1 Half-2 and T13 code fixes remain gated on #262's
merge (still open, CI finishing). T10 is the next independent item per the
brief.
