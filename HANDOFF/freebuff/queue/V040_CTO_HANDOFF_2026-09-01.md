# V040_CTO_HANDOFF_2026-09-01 -- CEO to CTO, v0.4.0 finale

Status: ACTIVE -- awaiting CTO acknowledgement and go/no-go on safe work-ahead
From: CEO seat
To: CTO (other session)
Date: 2026-09-01

## What I am doing in this seat

I am the CEO for the v0.4.0 finale. My job is to keep the endpoint honest:
decide the shape of the finish, confirm the rule-8 disposition, watch the
evidence, and escalate anything that is not 99%+ confidence to the CTO before
either of us acts.

I am not implementing the code in this seat. I am overseeing. The CTO owns
getting the actual work done and landing it on `origin/main`.

## Shared reality we must both work from

We are aligned on the target: **a final `v0.4.0` tag, after the last required
change lands, followed by functional validation.**

Current anchor points, all obtained by running commands in this session and
read from the repo:

- `origin/main` is at `67d19d3c`.
- Recent merges on `origin/main` in order:
  - `#259` `b2d8d126` v0.4.0 endgame plan, Freebuff lane, continuous-execution policy, keystore correction
  - `#260` `90372779` fix(docs): repair docs-sync gate
  - `#261` `9a45b3e7` docs: rule the T2 disk blocker; ledger the 41 GB target tree
  - `#262` `68e2c275` refactor(cli): unify CLI peer store onto the core LedgerManager
  - `#263` `bb253eaf` V040-T4: feed routing engine on ConnectionEstablished (D6)
  - `#266` `67d19d3c` feat(cli): boot-time seed dial with bounded backoff (T1 Half 2)
- My current checkout is `0e0d54da` on branch `cto/t2-disk-ruling-2026-08-31`. It
  is CEO/CTO handoff and rulings documentation on top of `origin/main`. It is **not**
  the build target for the fleet.

What is already delivered and merged into `origin/main`:
- T2 peer-store unification (#262)
- T4 routing feed on ConnectionEstablished (#263)
- T1 Half 2 boot-time seed dial (#266)

What is open and still needs to move before the tag:
- PR #271 T8 restore diagnostics formatter test -- CI-only, already CI-verified
  30/30 green. No Rule-8 gate.
- PR #267 T13 Part A (F1 + F2 + F-DHT Option A) -- Rule-8 gated.
- PR #268 T13 Part B (F7 Option B + C) -- Rule-8 gated.
- PR #264 T12 CI -- authored/in-review only; blocks T9.
- PR #269, #270 -- already reviewed in the lane; dispositions filed; APPROVE pending.

The rest of the v0.4.0 gates are not code blockers in this round:
- D1 main green: targeted ref must be green.
- D2 signed APK: operator keystore + secrets.
- D3 README: already OK.
- D4/D6/D7: functional validation after the fleet is rebuilt and the release
  signing path is available.
- G3-0 churn gate: redeploy AWS to a new IP and prove unaided re-mesh.

## The only contested code item

F7 Option B: widen `NeighborhoodSummary.reachable_hints` from `[u8; 4]` to
`[u8; 8]` in `core/src/routing/neighborhood.rs:75-76`.

This is the one I want both of us agreeing on before we touch it:

- It is a wire-format change. `NeighborhoodSummary` is gossiped via
  `NeighborhoodGossip`, so old and new builds cannot exchange hints meaningfully.
- Persisted state: there are no old-width persisted summaries in the current
  cutover scope. The lane's T13 Part B packet says this is a pure cutover, no
  migration. I am aligning on that with the CTO. If either of us is not 99%+
  confident on that statement, we stop and verify before we write the PR body.
- Timing: it must land before the tag. There is no deployed installed base, so it
  is cheap now and materially more expensive after shipping.
- PR body must call out the 3-node coordinated restart as the gate: AWS + Windows
  CLI + Android move together.
- Rule-8: this touches `core/src/routing/` and `core/src/transport/`. Adversarial
  review is required before merge, from a reviewer that did not author the change.

## My decision on persisted state

I am aligning on the lane's current stated position: **no old-width persisted
summaries in scope; pure cutover; PR documents the 3-node restart as the gate.**

If either of us finds evidence that old-width persisted summaries do exist in the
cutover path, we treat that as a blocker and re-plan rather than ship a
half-migration.

## Rule-8 dispatch I am writing now

I am dispatching the adversarial review for T13 Part B (F7 Option B + C, PR #268)
as its own packet.

Selection logic, per the repo philosophy cannon:

- This is adversarial review of merge-blocked code, not implementation. It belongs
  in a reasoning/review tier, not a micro lane.
- I am routing through the Qtier/CQO-free Qwen adversarial-review path the project
  already uses for this class of work, and I am pinning the exact model.
- I am not using the paid Qwen lane. It was removed by operator ruling 2026-08-31.
- I am not using a bare model alias. I am using a dated pin from the allowlist.
- The packet records the dispatch reference, the exact files/line ranges under review,
  the rule-8 boundary, the required outcome, and the acceptance contract.

I am writing that packet to `HANDOFF/freebuff/queue/` and recording it in
`HANDOFF/freebuff/README.md` so the lane can see it from `main`.

The same philosophy alignment applies to the rest of the open PRs: if a review is
required and not yet landed, the packet for it must be explicit about role, files,
gates, acceptance, and provider/model pinning.

## What I need the CTO to do

1. Acknowledge this handoff and confirm you are working from `origin/main`
   `67d19d3c` or a later merged main, not from my handoff branch.
2. Own the execution path to the tag. I am giving you the shape; you figure out
   the safe ordering, the CI/merge strategy, the worktree discipline, and how to
   get each required item landed.
3. Run the Rule-8 review dispatch for #268, and any other required review that is
   still outstanding, through the orchestrated lane with the same philosophy
   alignment I used for the packet I just wrote.
4. Rebuild/redeploy the three nodes to the same target SHA when we are ready to
   score: Windows CLI, AWS, Android.
5. Coordinate the release-signing posture with the operator when we get there:
   throwaway release key for functional validation now, real key for the published
   artifact later.
6. Leave me a clear signal when:
   - a task is safe to start now
   - a task needs my assessment before moving
   - a task is not 99%+ confidence and needs us to confer

## What I will do while you are executing

- Keep the target ref, the gate list, and the buyoff checklist current.
- Confirm evidence for anything I claim, command output or run URL, not a summary.
- Escalate to the CTO any item I am not 99%+ confident on before I act on it.
- Hold the line on the tag-finale sequence: last required change lands, then
  functional validation, then the tag.

## Safe work-ahead allowance

You have my go-ahead for anything that is:
- obviously safe work-ahead on already-approved scope
- clearly aligned with the planning I just restated
- not a judgement call I have reserved for myself

If you are not 99%+ confident that an item is safe work-ahead, do not start it.
Ask me. If I am not 99%+ confident either, we confer before either of us moves.

## CEO/CTO alignment statement

We are aligned on this move-forward plan:

- Final `v0.4.0` tag, after the last required change lands.
- Functional validation first on a release-configured build; published-artifact
  validation when the real signing path is available.
- F7 Option B lands before the tag; PR documents the 3-node coordinated restart.
- Persisted state for F7 is a pure cutover per the current lane packet; if that is
  wrong, we stop and re-plan.
- Rule-8 adversarial reviews are dispatched through the orchestrated lane with the
  repo philosophy cannon applied: no bare aliases, no paid-lane resurrection, pinned
  capable model, explicit files/gates/acceptance.
- I oversee; you execute. If either of us is not 99%+ confident, we escalate to the
  other before acting.

## Go/no-go signal I will wait for

I am not pushing the tag myself. I am waiting for the CTO to confirm:
- the last required change is landed and reviewed
- the fleet is rebuilt on the same target SHA
- the functional validation is scored, or the failure is reduced to a single ticket

Then I will make the tag call.
