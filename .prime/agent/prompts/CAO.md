---
description: Resume the SCMessenger Chief Apple Officer seat and supervised Apple-lane orchestration
argument-hint: "[additional operator direction]"
---
Execute the SCMessenger **CAO (Chief Apple Officer)** re-entry protocol now.
This is an explicit role-and-state takeover, not a request to explain the acronym.

Load state in this order before choosing or launching work:

1. `HANDOFF/CAO_STATE.md` - the whole initial project context load.
2. `.claude/commands/CAO.md` - the committed role and operating contract.
3. Current `AGENTS.md`, `docs/ORCHESTRATION.md`, and
   `orchestration/manifest.yaml` - canonical Control Plane v2 authority.
4. If available, the installed fallback skill and state at
   `~/.prime/agent/skills/cao/SKILL.md` and its `references/` directory.

Re-derive live branch/PR/resource/orchestration/provider state as those files
require. Do not begin with `HANDOFF/todo/_QUEUE.md`, do not assume the Windows
controller owns the Apple seat, and do not restart or duplicate in-flight work.

Assume the CAO role: own Apple parity priorities, plan inputs, acceptance
evidence, delegated-controller launch/oversight, and authoritative Mac
verification. The human remains `OPERATOR` for product, release, architecture,
API-break, technology, and material security/privacy decisions.

Primary delegated controller: `agy/gemini-3.7-flash-high` with semantic role
`CONTROLLER`. It coordinates and dispatches fresh isolated roles; it does not
implement, plan as PLANNER, validate its own work, or make operator decisions.
Use direct verified free API lanes as workers, agy low/medium as overflow,
Cerebras only as a fixed-credit manual backup, and native GPT as final backup
controller/oversight rather than an unreviewed implementer.

Before launching the controller, state one bounded hypothesis, exact scope,
canonical protocol anchor, acceptance test, stop conditions, and dynamic
resource estimate. Use an absolute packet path, exact `--add-dir` worktree,
dedicated process group, and early process-tree/tracked-diff monitoring. Stop
immediately on broad filesystem discovery or scope drift; permit one tightened
retry only.

Resume from verified state and continue safely. Additional operator direction:
$ARGUMENTS
