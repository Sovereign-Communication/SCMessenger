# Bob adapter

Status: Active
Last updated: 2026-08-15

Read `AGENTS.md` at the repo root -- that file is the canonical contract.
Bob is an adapter, not an alternate authority.

This directory contains:
- `skills/`: Bob skill definitions (gemini-orchestrator, orchestrate, scm, scmorc, scmqwen, swarm)

All Bob skill executions and agent delegations must adhere strictly to the
universal contract defined in root `AGENTS.md`.
